# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from .raw_client import AsyncRawConversationsClient, RawConversationsClient
from .types.cancel_conversations_response import CancelConversationsResponse
from .types.cancel_queued_conversations_response import CancelQueuedConversationsResponse
from .types.get_conversation_call_id_recording_download_url_response import (
    GetConversationCallIdRecordingDownloadUrlResponse,
)
from .types.list_retry_attempts_conversations_response import ListRetryAttemptsConversationsResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ConversationsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawConversationsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawConversationsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawConversationsClient
        """
        return self._raw_client

    def get_a_time_limited_recording_download_url(
        self, call_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GetConversationCallIdRecordingDownloadUrlResponse:
        """
        Returns a presigned S3 URL for the call's recording. Hand the URL straight to the customer or pull bytes server-side. The presigned URL is **time-limited** — typically usable for a few minutes — so don't cache it; request a fresh one each time you need the recording.

        Returns `404` if the call has no recording (call hasn't started, was cancelled before audio captured, or was deleted by the platform's retention policy). Returns `400 Invalid call ID format` if you pass a Mongo `_id` instead of the `callId` string.

        Parameters
        ----------
        call_id : str
            The `callId` string for the conversation (e.g. `CALL-1778226705739-7e4c17`). This is the `callId` field returned by `GET /conversation`, **not** the Mongo `_id` — passing `_id` returns `400 Invalid call ID format`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetConversationCallIdRecordingDownloadUrlResponse
            Successful response — presigned URL ready to fetch.

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.conversations.get_a_time_limited_recording_download_url(
            call_id="CALL-1781127346211-e765f7",
        )
        """
        _response = self._raw_client.get_a_time_limited_recording_download_url(call_id, request_options=request_options)
        return _response.data

    def list_retry_attempts(
        self, call_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ListRetryAttemptsConversationsResponse:
        """
        Returns the **parent call** plus every retry attempt that branched from it, ordered by attempt index. Use this when a customer asks "did the platform retry this call?" — typically driven by an outbound agent's auto-retry configuration (`maxRetries`, `retryDelay`).

        - If the `callId` you pass is the original (parent), the response contains that parent plus all child retries.
        - If the `callId` you pass is itself a retry, the response still includes the parent and every sibling retry — the API resolves to the family root automatically.

        Returns `404` if no call exists with that ID in your organization.

        Parameters
        ----------
        call_id : str
            Any `callId` in the retry family (parent or any retry).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListRetryAttemptsConversationsResponse
            Successful response — full retry family.

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.conversations.list_retry_attempts(
            call_id="callId",
        )
        """
        _response = self._raw_client.list_retry_attempts(call_id, request_options=request_options)
        return _response.data

    def cancel(
        self,
        *,
        call_id: str,
        reason: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CancelConversationsResponse:
        """
        Cancels an outbound call that has been queued or is in progress. Use the body form to look the call up by `callId`; the path-param form (`POST /conversation/{callId}/cancel`) is the equivalent for REST conventions, but only handles `IN_QUEUE` calls.

        Returns `404` if no call with that ID exists in your organization. Returns `400` if the call is already in a terminal state (completed / failed / cancelled).

        Parameters
        ----------
        call_id : str
            The `callId` returned by `POST /conversation/outbound` or visible in `GET /conversation`.

        reason : typing.Optional[str]
            Optional free-text reason for cancellation. Logged for support / audit.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CancelConversationsResponse
            Call cancelled.

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.conversations.cancel(
            call_id="CALL-1778226705739-7e4c17",
        )
        """
        _response = self._raw_client.cancel(call_id=call_id, reason=reason, request_options=request_options)
        return _response.data

    def cancel_queued(
        self, call_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> CancelQueuedConversationsResponse:
        """
        REST-conventional path-param variant of [`POST /conversation/cancel`](#operation/cancelCallByBody).

        **Behavior differs from the body form.** This path-param endpoint only cancels calls that are still in the `IN_QUEUE` state — calls that have already started dialing or are in progress return `400 Bad Request` with `errors: ["Conversation with ID ... is not in queue and cannot be cancelled"]`. Use the body form (`POST /conversation/cancel`) if you need to cancel an in-progress call.

        The path param is the `callId` string (e.g. `CALL-1778226705739-7e4c17`), **not** the Mongo `_id`. Passing `_id` returns `404 No conversation found`.

        Parameters
        ----------
        call_id : str
            The `callId` string for the conversation to cancel.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CancelQueuedConversationsResponse
            Call cancelled.

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.conversations.cancel_queued(
            call_id="CALL-1781127346211-e765f7",
        )
        """
        _response = self._raw_client.cancel_queued(call_id, request_options=request_options)
        return _response.data


class AsyncConversationsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawConversationsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawConversationsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawConversationsClient
        """
        return self._raw_client

    async def get_a_time_limited_recording_download_url(
        self, call_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GetConversationCallIdRecordingDownloadUrlResponse:
        """
        Returns a presigned S3 URL for the call's recording. Hand the URL straight to the customer or pull bytes server-side. The presigned URL is **time-limited** — typically usable for a few minutes — so don't cache it; request a fresh one each time you need the recording.

        Returns `404` if the call has no recording (call hasn't started, was cancelled before audio captured, or was deleted by the platform's retention policy). Returns `400 Invalid call ID format` if you pass a Mongo `_id` instead of the `callId` string.

        Parameters
        ----------
        call_id : str
            The `callId` string for the conversation (e.g. `CALL-1778226705739-7e4c17`). This is the `callId` field returned by `GET /conversation`, **not** the Mongo `_id` — passing `_id` returns `400 Invalid call ID format`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetConversationCallIdRecordingDownloadUrlResponse
            Successful response — presigned URL ready to fetch.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.conversations.get_a_time_limited_recording_download_url(
                call_id="CALL-1781127346211-e765f7",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_a_time_limited_recording_download_url(
            call_id, request_options=request_options
        )
        return _response.data

    async def list_retry_attempts(
        self, call_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ListRetryAttemptsConversationsResponse:
        """
        Returns the **parent call** plus every retry attempt that branched from it, ordered by attempt index. Use this when a customer asks "did the platform retry this call?" — typically driven by an outbound agent's auto-retry configuration (`maxRetries`, `retryDelay`).

        - If the `callId` you pass is the original (parent), the response contains that parent plus all child retries.
        - If the `callId` you pass is itself a retry, the response still includes the parent and every sibling retry — the API resolves to the family root automatically.

        Returns `404` if no call exists with that ID in your organization.

        Parameters
        ----------
        call_id : str
            Any `callId` in the retry family (parent or any retry).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListRetryAttemptsConversationsResponse
            Successful response — full retry family.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.conversations.list_retry_attempts(
                call_id="callId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_retry_attempts(call_id, request_options=request_options)
        return _response.data

    async def cancel(
        self,
        *,
        call_id: str,
        reason: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CancelConversationsResponse:
        """
        Cancels an outbound call that has been queued or is in progress. Use the body form to look the call up by `callId`; the path-param form (`POST /conversation/{callId}/cancel`) is the equivalent for REST conventions, but only handles `IN_QUEUE` calls.

        Returns `404` if no call with that ID exists in your organization. Returns `400` if the call is already in a terminal state (completed / failed / cancelled).

        Parameters
        ----------
        call_id : str
            The `callId` returned by `POST /conversation/outbound` or visible in `GET /conversation`.

        reason : typing.Optional[str]
            Optional free-text reason for cancellation. Logged for support / audit.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CancelConversationsResponse
            Call cancelled.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.conversations.cancel(
                call_id="CALL-1778226705739-7e4c17",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.cancel(call_id=call_id, reason=reason, request_options=request_options)
        return _response.data

    async def cancel_queued(
        self, call_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> CancelQueuedConversationsResponse:
        """
        REST-conventional path-param variant of [`POST /conversation/cancel`](#operation/cancelCallByBody).

        **Behavior differs from the body form.** This path-param endpoint only cancels calls that are still in the `IN_QUEUE` state — calls that have already started dialing or are in progress return `400 Bad Request` with `errors: ["Conversation with ID ... is not in queue and cannot be cancelled"]`. Use the body form (`POST /conversation/cancel`) if you need to cancel an in-progress call.

        The path param is the `callId` string (e.g. `CALL-1778226705739-7e4c17`), **not** the Mongo `_id`. Passing `_id` returns `404 No conversation found`.

        Parameters
        ----------
        call_id : str
            The `callId` string for the conversation to cancel.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CancelQueuedConversationsResponse
            Call cancelled.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.conversations.cancel_queued(
                call_id="CALL-1781127346211-e765f7",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.cancel_queued(call_id, request_options=request_options)
        return _response.data
