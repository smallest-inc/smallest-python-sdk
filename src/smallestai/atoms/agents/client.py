# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from ..types.single_prompt_config import SinglePromptConfig
from ..types.workflow_type import WorkflowType
from .raw_client import AsyncRawAgentsClient, RawAgentsClient
from .types.archive_agent_agents_response import ArchiveAgentAgentsResponse
from .types.create_agent_agents_response import CreateAgentAgentsResponse
from .types.create_agent_request_background_sound import CreateAgentRequestBackgroundSound
from .types.create_agent_request_denoising_config import CreateAgentRequestDenoisingConfig
from .types.create_agent_request_language import CreateAgentRequestLanguage
from .types.create_agent_request_llm_idle_timeout_config import CreateAgentRequestLlmIdleTimeoutConfig
from .types.create_agent_request_pre_call_api import CreateAgentRequestPreCallApi
from .types.create_agent_request_pronunciation_dicts_item import CreateAgentRequestPronunciationDictsItem
from .types.create_agent_request_redaction_config import CreateAgentRequestRedactionConfig
from .types.create_agent_request_session_timeout_config import CreateAgentRequestSessionTimeoutConfig
from .types.create_agent_request_slm_model import CreateAgentRequestSlmModel
from .types.create_agent_request_smart_turn_config import CreateAgentRequestSmartTurnConfig
from .types.create_agent_request_synthesizer import CreateAgentRequestSynthesizer
from .types.create_agent_request_timezone import CreateAgentRequestTimezone
from .types.create_agent_request_voice_detection_config import CreateAgentRequestVoiceDetectionConfig
from .types.create_agent_request_voice_mail_detection_config import CreateAgentRequestVoiceMailDetectionConfig
from .types.duplicate_agent_agents_response import DuplicateAgentAgentsResponse
from .types.get_agent_agents_response import GetAgentAgentsResponse
from .types.get_agent_id_workflow_response import GetAgentIdWorkflowResponse
from .types.list_agents_agents_request_sort_field import ListAgentsAgentsRequestSortField
from .types.list_agents_agents_request_sort_order import ListAgentsAgentsRequestSortOrder
from .types.list_agents_agents_request_type import ListAgentsAgentsRequestType
from .types.list_agents_agents_response import ListAgentsAgentsResponse
from .types.update_agent_agents_response import UpdateAgentAgentsResponse
from .types.update_workflow_configuration_agents_request_workflow_graph import (
    UpdateWorkflowConfigurationAgentsRequestWorkflowGraph,
)

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AgentsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAgentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAgentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAgentsClient
        """
        return self._raw_client

    def list_agents(
        self,
        *,
        page: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        type: typing.Optional[ListAgentsAgentsRequestType] = None,
        sort_field: typing.Optional[ListAgentsAgentsRequestSortField] = None,
        sort_order: typing.Optional[ListAgentsAgentsRequestSortOrder] = None,
        archived: typing.Optional[bool] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListAgentsAgentsResponse:
        """
        Agents are the main entities in the system. Agents are used to create conversations. You can create workflow for an agent and configure it for different use cases. You can also create custom workflows for an agent. This API will give you the list of agents created by organization you are a part of.

        Parameters
        ----------
        page : typing.Optional[int]

        offset : typing.Optional[int]

        search : typing.Optional[str]

        type : typing.Optional[ListAgentsAgentsRequestType]
            Filter agents by workflow type

        sort_field : typing.Optional[ListAgentsAgentsRequestSortField]
            Field to sort results by

        sort_order : typing.Optional[ListAgentsAgentsRequestSortOrder]
            Sort direction

        archived : typing.Optional[bool]
            When true, returns only archived agents. Omit or set to false to return active agents.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListAgentsAgentsResponse
            Successful response

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            token="YOUR_TOKEN",
        )
        client.atoms.agents.list_agents()
        """
        _response = self._raw_client.list_agents(
            page=page,
            offset=offset,
            search=search,
            type=type,
            sort_field=sort_field,
            sort_order=sort_order,
            archived=archived,
            request_options=request_options,
        )
        return _response.data

    def create_agent(
        self,
        *,
        name: str,
        description: typing.Optional[str] = OMIT,
        background_sound: typing.Optional[CreateAgentRequestBackgroundSound] = OMIT,
        language: typing.Optional[CreateAgentRequestLanguage] = OMIT,
        synthesizer: typing.Optional[CreateAgentRequestSynthesizer] = OMIT,
        global_knowledge_base_id: typing.Optional[str] = OMIT,
        slm_model: typing.Optional[CreateAgentRequestSlmModel] = OMIT,
        default_variables: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        pre_call_api: typing.Optional[CreateAgentRequestPreCallApi] = OMIT,
        global_prompt: typing.Optional[str] = OMIT,
        telephony_product_id: typing.Optional[typing.Sequence[str]] = OMIT,
        workflow_type: typing.Optional[WorkflowType] = OMIT,
        first_message: typing.Optional[str] = OMIT,
        mute_user_until_first_bot_response: typing.Optional[bool] = OMIT,
        allow_interruptions: typing.Optional[bool] = OMIT,
        wait_for_user_to_speak_first: typing.Optional[bool] = OMIT,
        interruption_backoff_timer: typing.Optional[float] = OMIT,
        smart_turn_config: typing.Optional[CreateAgentRequestSmartTurnConfig] = OMIT,
        voice_detection_config: typing.Optional[CreateAgentRequestVoiceDetectionConfig] = OMIT,
        voice_mail_detection_config: typing.Optional[CreateAgentRequestVoiceMailDetectionConfig] = OMIT,
        denoising_config: typing.Optional[CreateAgentRequestDenoisingConfig] = OMIT,
        redaction_config: typing.Optional[CreateAgentRequestRedactionConfig] = OMIT,
        pronunciation_dicts: typing.Optional[typing.Sequence[CreateAgentRequestPronunciationDictsItem]] = OMIT,
        llm_idle_timeout_config: typing.Optional[CreateAgentRequestLlmIdleTimeoutConfig] = OMIT,
        session_timeout_config: typing.Optional[CreateAgentRequestSessionTimeoutConfig] = OMIT,
        timezone: typing.Optional[CreateAgentRequestTimezone] = OMIT,
        call_disposition_config: typing.Optional[str] = OMIT,
        allow_inbound_call: typing.Optional[bool] = OMIT,
        enable_style_guide: typing.Optional[bool] = OMIT,
        speech_formatting: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateAgentAgentsResponse:
        """
        Create a new agent by passing the agent name in the request body.

        New agents have versioning enabled by default. To set the prompt,
        `firstMessage`, tools, or any runtime config, fork a draft from the
        auto-created initial version, edit it, publish, and activate — see
        the [Versioning Lifecycle](/atoms/developer-guide/build/agents/versioning-lifecycle)
        guide for the full flow.

        The legacy `PATCH /workflow/{workflowId}` endpoint writes directly to
        the underlying workflow document and bypasses the version lifecycle;
        edits made that way are not captured as a version and may not
        propagate to live calls. Use the drafts flow above.

        Parameters
        ----------
        name : str

        description : typing.Optional[str]

        background_sound : typing.Optional[CreateAgentRequestBackgroundSound]
            Ambient background sound during calls. Options: '' (none), 'office', 'cafe', 'call_center', 'static'. Note: this value is currently overridden by the server default on creation; update via PATCH after creation.

        language : typing.Optional[CreateAgentRequestLanguage]
            Language configuration for the agent.
            Cross-field rule: `default` must be one of the values in `supported`.
            Tamil (`ta`) cannot be combined with other languages in `supported`.

        synthesizer : typing.Optional[CreateAgentRequestSynthesizer]
            Synthesizer (TTS) configuration for the agent.
            Models `waves`, `waves_lightning_large`, `waves_lightning_v2`, and `waves_lightning_v3_1`
            validate `voiceId` against the Waves API. All other models accept any voiceId.
            Cloned voices are regular voiceIds — use them with any compatible Waves model.

        global_knowledge_base_id : typing.Optional[str]
            The global knowledge base ID of the agent. You can create a global knowledge base by using the /knowledgebase endpoint and assign it to the agent. The agent will use this knowledge base for its responses.

        slm_model : typing.Optional[CreateAgentRequestSlmModel]
            The LLM model to use for the agent.
            Note: `gpt-5.2`, `electron-kogta`, and `electron-kogta-v2` require org-level access and return 403 if not enabled.
            `workflowType` must be `single_prompt` to use `gpt-realtime` or `gpt-realtime-mini`.

        default_variables : typing.Optional[typing.Dict[str, typing.Any]]
            The default variables to use for the agent. These variables will be used if no variables are provided when initiating a conversation with the agent.

        pre_call_api : typing.Optional[CreateAgentRequestPreCallApi]
            Configuration for an API call to be made before the call starts. The response variables can be injected into the agent's prompt.

        global_prompt : typing.Optional[str]
            Set global instructions for your agent's personality, role, and behavior throughout conversations.
            Note: Only used for workflow_graph agents. Maximum 4000 characters.

        telephony_product_id : typing.Optional[typing.Sequence[str]]
            IDs of telephony products (phone numbers) to associate with the agent for inbound/outbound calls.

        workflow_type : typing.Optional[WorkflowType]
            The type of workflow to create for the agent. Defaults to `single_prompt` if not specified. Using `workflow_graph` requires conversational agent access (403 if not enabled).

        first_message : typing.Optional[str]
            The first message the agent sends when a conversation starts.

        mute_user_until_first_bot_response : typing.Optional[bool]
            When true, the user's audio is muted until the agent has finished its first response.

        allow_interruptions : typing.Optional[bool]
            Whether the user can interrupt the agent while it is speaking.

        wait_for_user_to_speak_first : typing.Optional[bool]
            When true, the agent waits for the user to speak before sending the first message.

        interruption_backoff_timer : typing.Optional[float]
            Seconds the agent waits after being interrupted before resuming speech.

        smart_turn_config : typing.Optional[CreateAgentRequestSmartTurnConfig]
            Smart turn-detection configuration. When enabled, the agent uses an additional model to decide whether the user has finished a turn.

        voice_detection_config : typing.Optional[CreateAgentRequestVoiceDetectionConfig]
            Voice activity detection (VAD) configuration. Controls how the agent decides when speech is present.

        voice_mail_detection_config : typing.Optional[CreateAgentRequestVoiceMailDetectionConfig]
            Voicemail-detection configuration. When the call hits a voicemail tone, the agent plays `endText` and ends the call.

        denoising_config : typing.Optional[CreateAgentRequestDenoisingConfig]
            Background-noise denoising configuration for the agent's input audio.

        redaction_config : typing.Optional[CreateAgentRequestRedactionConfig]
            PII redaction configuration. When enabled, personally identifiable information is redacted from transcripts before storage.

        pronunciation_dicts : typing.Optional[typing.Sequence[CreateAgentRequestPronunciationDictsItem]]
            Pronunciation overrides — words the TTS engine should pronounce differently from its default.

        llm_idle_timeout_config : typing.Optional[CreateAgentRequestLlmIdleTimeoutConfig]
            Timeout configuration for the LLM stage of a conversation. Triggers a retry or call termination when the LLM does not respond within the configured window.

        session_timeout_config : typing.Optional[CreateAgentRequestSessionTimeoutConfig]
            Maximum duration of a conversation session. The call ends after this elapsed time even if active.

        timezone : typing.Optional[CreateAgentRequestTimezone]
            Timezone applied to scheduled actions and timestamps the agent reports to the user.

        call_disposition_config : typing.Optional[str]
            Configuration string for call disposition tracking.

        allow_inbound_call : typing.Optional[bool]
            Whether the agent accepts inbound calls.

        enable_style_guide : typing.Optional[bool]
            Whether style guide enforcement is applied to agent responses.

        speech_formatting : typing.Optional[bool]
            Whether speech formatting is applied to the agent's responses.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateAgentAgentsResponse
            Agent created successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            token="YOUR_TOKEN",
        )
        client.atoms.agents.create_agent(
            name="name",
        )
        """
        _response = self._raw_client.create_agent(
            name=name,
            description=description,
            background_sound=background_sound,
            language=language,
            synthesizer=synthesizer,
            global_knowledge_base_id=global_knowledge_base_id,
            slm_model=slm_model,
            default_variables=default_variables,
            pre_call_api=pre_call_api,
            global_prompt=global_prompt,
            telephony_product_id=telephony_product_id,
            workflow_type=workflow_type,
            first_message=first_message,
            mute_user_until_first_bot_response=mute_user_until_first_bot_response,
            allow_interruptions=allow_interruptions,
            wait_for_user_to_speak_first=wait_for_user_to_speak_first,
            interruption_backoff_timer=interruption_backoff_timer,
            smart_turn_config=smart_turn_config,
            voice_detection_config=voice_detection_config,
            voice_mail_detection_config=voice_mail_detection_config,
            denoising_config=denoising_config,
            redaction_config=redaction_config,
            pronunciation_dicts=pronunciation_dicts,
            llm_idle_timeout_config=llm_idle_timeout_config,
            session_timeout_config=session_timeout_config,
            timezone=timezone,
            call_disposition_config=call_disposition_config,
            allow_inbound_call=allow_inbound_call,
            enable_style_guide=enable_style_guide,
            speech_formatting=speech_formatting,
            request_options=request_options,
        )
        return _response.data

    def get_agent(
        self,
        id: str,
        *,
        draft_id: typing.Optional[str] = None,
        version_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetAgentAgentsResponse:
        """
        Returns the agent document merged with the resolved config of the active version
        under `_resolvedConfig`. Non-versioned fields (name, telephonyProductId, allowInboundCall,
        etc.) sit at the top level; versioned fields (prompt, tools, language, synthesizer,
        post-call analytics, …) are resolved from the target version and exposed under `_resolvedConfig`.

        **Previewing a draft or specific version**

        Pass `?draftId=<id>` to resolve config from a specific draft instead of the active version.
        Pass `?versionId=<id>` to resolve config from a specific published version.
        When either param is used, the response includes `_configSource: "draft" | "version" | "active"`
        indicating which config was resolved.

        Notable resolved fields in `_resolvedConfig`:

        - `prompt` — active version's single-prompt text
        - `tools` — configured tools on the resolved version
        - `postCallAnalyticsConfig` — disposition metrics + analytics model flags
        - `modelName` — LLM model name on the resolved version
        - `defaultLanguage`, `supportedLanguages` — active language config
        - `firstMessage`, `globalPrompt` — active messaging config
        - `workflowGraph` — full node graph for `workflow_graph` agents

        To read prompt + tools alone, use `GET /agent/{id}/workflow` (deprecated for
        new integrations but still live). To inspect a specific non-active version,
        use `GET /agent/{id}/versions/{versionId}`.

        **400 — also used for "not found":** if the agent ID does not exist in the
        organization, the API returns 400 with `errors: ["No agent found"]` rather than 404.

        Parameters
        ----------
        id : str

        draft_id : typing.Optional[str]
            Resolve `_resolvedConfig` from this draft instead of the active version. Sets `_configSource` to `"draft"` in the response.

        version_id : typing.Optional[str]
            Resolve `_resolvedConfig` from this published version instead of the active version. Sets `_configSource` to `"version"` in the response.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentAgentsResponse
            Successful response

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            token="YOUR_TOKEN",
        )
        client.atoms.agents.get_agent(
            id="id",
        )
        """
        _response = self._raw_client.get_agent(
            id, draft_id=draft_id, version_id=version_id, request_options=request_options
        )
        return _response.data

    def update_agent(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        avatar_url: typing.Optional[str] = OMIT,
        telephony_product_id: typing.Optional[typing.Sequence[str]] = OMIT,
        allow_inbound_call: typing.Optional[bool] = OMIT,
        visible_to_everyone: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateAgentAgentsResponse:
        """
        Update agent fields. Behavior depends on whether the agent has versioning enabled:

        **Versioned agents** (have an active published version): only metadata fields are accepted —
        `name`, `description`, `avatarUrl`, `telephonyProductId`, `allowInboundCall`, `visibleToEveryone`.
        Submitting any config-level field returns 400 with
        `"Agent has versioning enabled. Config changes must be made through drafts."`.
        Use `PATCH /agent/{id}/drafts/{draftId}/config` instead.

        **Non-versioned agents** (no active version): all configuration fields are accepted,
        the same full set as `POST /agent`.

        **400 is also returned when:**
        - The agent is locked (`"Agent is locked, please unlock it to update"`)
        - Cross-field constraint violated (e.g. `north_indic` language requires `transcriberType: pulse`)

        **403** is returned when selecting a gated model (`gpt-5.2`, `electron-kogta`, `electron-kogta-v2`)
        without org-level access.

        Parameters
        ----------
        id : str

        name : typing.Optional[str]
            Name of the agent.

        description : typing.Optional[str]
            Description of the agent.

        avatar_url : typing.Optional[str]
            URL of the agent's avatar image.

        telephony_product_id : typing.Optional[typing.Sequence[str]]
            IDs of telephony products (phone numbers) to associate with the agent.

        allow_inbound_call : typing.Optional[bool]
            Whether the agent accepts inbound calls.

        visible_to_everyone : typing.Optional[bool]
            Whether the agent is visible to all members of the organization.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateAgentAgentsResponse
            Agent updated successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            token="YOUR_TOKEN",
        )
        client.atoms.agents.update_agent(
            id="id",
        )
        """
        _response = self._raw_client.update_agent(
            id,
            name=name,
            description=description,
            avatar_url=avatar_url,
            telephony_product_id=telephony_product_id,
            allow_inbound_call=allow_inbound_call,
            visible_to_everyone=visible_to_everyone,
            request_options=request_options,
        )
        return _response.data

    def archive_agent(
        self, id: str, *, on: typing.Optional[bool] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> ArchiveAgentAgentsResponse:
        """
        Soft-archives the agent — it is excluded from listings and stops accepting calls,
        but all data is preserved and the operation is fully reversible.

        Pass `?on=false` to unarchive (restore) a previously archived agent.

        **409 is returned when:**
        - The agent is already in the requested state (`"Agent is already archived"` / `"Agent is already active"`)
        - The agent is linked to an active campaign (`"Agent is associated with the [status] campaign "[name]". Complete or remove the campaign before archiving."`)

        Parameters
        ----------
        id : str

        on : typing.Optional[bool]
            `true` (default) — archive the agent.
            `false` — unarchive (restore) a previously archived agent.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ArchiveAgentAgentsResponse
            Agent archived or unarchived successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            token="YOUR_TOKEN",
        )
        client.atoms.agents.archive_agent(
            id="id",
        )
        """
        _response = self._raw_client.archive_agent(id, on=on, request_options=request_options)
        return _response.data

    def duplicate_agent(
        self, id: str, *, target_organization_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> DuplicateAgentAgentsResponse:
        """
        Duplicates a SINGLE_PROMPT agent's live active version into a target organization
        (can also be the same organization). Copies all versioned configuration but strips
        organization-specific resources: knowledge base tools are removed, default variable
        values are blanked, and a new avatar is generated. The duplicate starts with a
        published V1 as its active version.

        **400 is returned when:**
        - The source agent is archived (`"Cannot duplicate an archived agent"`)
        - The agent has no `activeVersionId` (`"This agent has no active version and cannot be duplicated"`)
        - The active version exists but is not published/active (`"This agent has no active published version and cannot be duplicated"`)
        - The agent is not `SINGLE_PROMPT` workflow type

        Parameters
        ----------
        id : str
            The ID of the source agent to duplicate

        target_organization_id : str
            MongoDB ObjectId of the target organization. Must be a 24-character hex string.
            The authenticated user must be a member of this organization.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DuplicateAgentAgentsResponse
            Agent duplicated successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            token="YOUR_TOKEN",
        )
        client.atoms.agents.duplicate_agent(
            id="id",
            target_organization_id="60d0fe4f5311236168a109ca",
        )
        """
        _response = self._raw_client.duplicate_agent(
            id, target_organization_id=target_organization_id, request_options=request_options
        )
        return _response.data

    def get_agent_workflow(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GetAgentIdWorkflowResponse:
        """
        **Deprecated** — prefer `GET /agent/{id}` (config is resolved into `_resolvedConfig`
        including prompt, tools, and post-call analytics).

        Returns the active version's prompt and tools for single-prompt agents, or the
        workflow graph data for workflow_graph agents. Customers still rely on this to
        fetch their current prompt + tools — endpoint is kept live for now.

        **Caveat:** the `versionId` query param (if passed) is silently ignored.
        The response always reflects the currently-active version. To inspect a
        non-active version, use `GET /agent/{id}/versions/{versionId}`.

        Parameters
        ----------
        id : str
            The ID of the agent

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentIdWorkflowResponse
            Successful response

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            token="YOUR_TOKEN",
        )
        client.atoms.agents.get_agent_workflow(
            id="id",
        )
        """
        _response = self._raw_client.get_agent_workflow(id, request_options=request_options)
        return _response.data

    def update_workflow_configuration(
        self,
        id: str,
        *,
        type: WorkflowType,
        workflow_graph: typing.Optional[UpdateWorkflowConfigurationAgentsRequestWorkflowGraph] = OMIT,
        single_prompt_config: typing.Optional[SinglePromptConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        **Deprecated** — use `PATCH /agent/{id}/drafts/{draftId}/config` instead.

        Directly mutates the legacy workflow document for an agent. This write path
        bypasses the versioning system entirely: the change is not captured as a
        new version, and future version activations may overwrite the legacy doc
        back to whatever the version snapshot contains.

        ⚠ **Writing here on a versioned agent can silently wipe tools, prompt, or
        other fields that were missing from the PATCH payload.** Only use this if
        you know the agent is not using versioning, or if you are intentionally
        hot-patching the legacy doc.

        Parameters
        ----------
        id : str
            The workflow ID (found at `agent.workflowId` on the agent document).

        type : WorkflowType

        workflow_graph : typing.Optional[UpdateWorkflowConfigurationAgentsRequestWorkflowGraph]
            Required when `type = workflow_graph`. Exactly one of `workflowGraph` or `singlePromptConfig` must be provided.

        single_prompt_config : typing.Optional[SinglePromptConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            token="YOUR_TOKEN",
        )
        client.atoms.agents.update_workflow_configuration(
            id="60d0fe4f5311236168a109ca",
            type="workflow_graph",
        )
        """
        _response = self._raw_client.update_workflow_configuration(
            id,
            type=type,
            workflow_graph=workflow_graph,
            single_prompt_config=single_prompt_config,
            request_options=request_options,
        )
        return _response.data


class AsyncAgentsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAgentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAgentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAgentsClient
        """
        return self._raw_client

    async def list_agents(
        self,
        *,
        page: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        type: typing.Optional[ListAgentsAgentsRequestType] = None,
        sort_field: typing.Optional[ListAgentsAgentsRequestSortField] = None,
        sort_order: typing.Optional[ListAgentsAgentsRequestSortOrder] = None,
        archived: typing.Optional[bool] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListAgentsAgentsResponse:
        """
        Agents are the main entities in the system. Agents are used to create conversations. You can create workflow for an agent and configure it for different use cases. You can also create custom workflows for an agent. This API will give you the list of agents created by organization you are a part of.

        Parameters
        ----------
        page : typing.Optional[int]

        offset : typing.Optional[int]

        search : typing.Optional[str]

        type : typing.Optional[ListAgentsAgentsRequestType]
            Filter agents by workflow type

        sort_field : typing.Optional[ListAgentsAgentsRequestSortField]
            Field to sort results by

        sort_order : typing.Optional[ListAgentsAgentsRequestSortOrder]
            Sort direction

        archived : typing.Optional[bool]
            When true, returns only archived agents. Omit or set to false to return active agents.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListAgentsAgentsResponse
            Successful response

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atoms.agents.list_agents()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_agents(
            page=page,
            offset=offset,
            search=search,
            type=type,
            sort_field=sort_field,
            sort_order=sort_order,
            archived=archived,
            request_options=request_options,
        )
        return _response.data

    async def create_agent(
        self,
        *,
        name: str,
        description: typing.Optional[str] = OMIT,
        background_sound: typing.Optional[CreateAgentRequestBackgroundSound] = OMIT,
        language: typing.Optional[CreateAgentRequestLanguage] = OMIT,
        synthesizer: typing.Optional[CreateAgentRequestSynthesizer] = OMIT,
        global_knowledge_base_id: typing.Optional[str] = OMIT,
        slm_model: typing.Optional[CreateAgentRequestSlmModel] = OMIT,
        default_variables: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        pre_call_api: typing.Optional[CreateAgentRequestPreCallApi] = OMIT,
        global_prompt: typing.Optional[str] = OMIT,
        telephony_product_id: typing.Optional[typing.Sequence[str]] = OMIT,
        workflow_type: typing.Optional[WorkflowType] = OMIT,
        first_message: typing.Optional[str] = OMIT,
        mute_user_until_first_bot_response: typing.Optional[bool] = OMIT,
        allow_interruptions: typing.Optional[bool] = OMIT,
        wait_for_user_to_speak_first: typing.Optional[bool] = OMIT,
        interruption_backoff_timer: typing.Optional[float] = OMIT,
        smart_turn_config: typing.Optional[CreateAgentRequestSmartTurnConfig] = OMIT,
        voice_detection_config: typing.Optional[CreateAgentRequestVoiceDetectionConfig] = OMIT,
        voice_mail_detection_config: typing.Optional[CreateAgentRequestVoiceMailDetectionConfig] = OMIT,
        denoising_config: typing.Optional[CreateAgentRequestDenoisingConfig] = OMIT,
        redaction_config: typing.Optional[CreateAgentRequestRedactionConfig] = OMIT,
        pronunciation_dicts: typing.Optional[typing.Sequence[CreateAgentRequestPronunciationDictsItem]] = OMIT,
        llm_idle_timeout_config: typing.Optional[CreateAgentRequestLlmIdleTimeoutConfig] = OMIT,
        session_timeout_config: typing.Optional[CreateAgentRequestSessionTimeoutConfig] = OMIT,
        timezone: typing.Optional[CreateAgentRequestTimezone] = OMIT,
        call_disposition_config: typing.Optional[str] = OMIT,
        allow_inbound_call: typing.Optional[bool] = OMIT,
        enable_style_guide: typing.Optional[bool] = OMIT,
        speech_formatting: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateAgentAgentsResponse:
        """
        Create a new agent by passing the agent name in the request body.

        New agents have versioning enabled by default. To set the prompt,
        `firstMessage`, tools, or any runtime config, fork a draft from the
        auto-created initial version, edit it, publish, and activate — see
        the [Versioning Lifecycle](/atoms/developer-guide/build/agents/versioning-lifecycle)
        guide for the full flow.

        The legacy `PATCH /workflow/{workflowId}` endpoint writes directly to
        the underlying workflow document and bypasses the version lifecycle;
        edits made that way are not captured as a version and may not
        propagate to live calls. Use the drafts flow above.

        Parameters
        ----------
        name : str

        description : typing.Optional[str]

        background_sound : typing.Optional[CreateAgentRequestBackgroundSound]
            Ambient background sound during calls. Options: '' (none), 'office', 'cafe', 'call_center', 'static'. Note: this value is currently overridden by the server default on creation; update via PATCH after creation.

        language : typing.Optional[CreateAgentRequestLanguage]
            Language configuration for the agent.
            Cross-field rule: `default` must be one of the values in `supported`.
            Tamil (`ta`) cannot be combined with other languages in `supported`.

        synthesizer : typing.Optional[CreateAgentRequestSynthesizer]
            Synthesizer (TTS) configuration for the agent.
            Models `waves`, `waves_lightning_large`, `waves_lightning_v2`, and `waves_lightning_v3_1`
            validate `voiceId` against the Waves API. All other models accept any voiceId.
            Cloned voices are regular voiceIds — use them with any compatible Waves model.

        global_knowledge_base_id : typing.Optional[str]
            The global knowledge base ID of the agent. You can create a global knowledge base by using the /knowledgebase endpoint and assign it to the agent. The agent will use this knowledge base for its responses.

        slm_model : typing.Optional[CreateAgentRequestSlmModel]
            The LLM model to use for the agent.
            Note: `gpt-5.2`, `electron-kogta`, and `electron-kogta-v2` require org-level access and return 403 if not enabled.
            `workflowType` must be `single_prompt` to use `gpt-realtime` or `gpt-realtime-mini`.

        default_variables : typing.Optional[typing.Dict[str, typing.Any]]
            The default variables to use for the agent. These variables will be used if no variables are provided when initiating a conversation with the agent.

        pre_call_api : typing.Optional[CreateAgentRequestPreCallApi]
            Configuration for an API call to be made before the call starts. The response variables can be injected into the agent's prompt.

        global_prompt : typing.Optional[str]
            Set global instructions for your agent's personality, role, and behavior throughout conversations.
            Note: Only used for workflow_graph agents. Maximum 4000 characters.

        telephony_product_id : typing.Optional[typing.Sequence[str]]
            IDs of telephony products (phone numbers) to associate with the agent for inbound/outbound calls.

        workflow_type : typing.Optional[WorkflowType]
            The type of workflow to create for the agent. Defaults to `single_prompt` if not specified. Using `workflow_graph` requires conversational agent access (403 if not enabled).

        first_message : typing.Optional[str]
            The first message the agent sends when a conversation starts.

        mute_user_until_first_bot_response : typing.Optional[bool]
            When true, the user's audio is muted until the agent has finished its first response.

        allow_interruptions : typing.Optional[bool]
            Whether the user can interrupt the agent while it is speaking.

        wait_for_user_to_speak_first : typing.Optional[bool]
            When true, the agent waits for the user to speak before sending the first message.

        interruption_backoff_timer : typing.Optional[float]
            Seconds the agent waits after being interrupted before resuming speech.

        smart_turn_config : typing.Optional[CreateAgentRequestSmartTurnConfig]
            Smart turn-detection configuration. When enabled, the agent uses an additional model to decide whether the user has finished a turn.

        voice_detection_config : typing.Optional[CreateAgentRequestVoiceDetectionConfig]
            Voice activity detection (VAD) configuration. Controls how the agent decides when speech is present.

        voice_mail_detection_config : typing.Optional[CreateAgentRequestVoiceMailDetectionConfig]
            Voicemail-detection configuration. When the call hits a voicemail tone, the agent plays `endText` and ends the call.

        denoising_config : typing.Optional[CreateAgentRequestDenoisingConfig]
            Background-noise denoising configuration for the agent's input audio.

        redaction_config : typing.Optional[CreateAgentRequestRedactionConfig]
            PII redaction configuration. When enabled, personally identifiable information is redacted from transcripts before storage.

        pronunciation_dicts : typing.Optional[typing.Sequence[CreateAgentRequestPronunciationDictsItem]]
            Pronunciation overrides — words the TTS engine should pronounce differently from its default.

        llm_idle_timeout_config : typing.Optional[CreateAgentRequestLlmIdleTimeoutConfig]
            Timeout configuration for the LLM stage of a conversation. Triggers a retry or call termination when the LLM does not respond within the configured window.

        session_timeout_config : typing.Optional[CreateAgentRequestSessionTimeoutConfig]
            Maximum duration of a conversation session. The call ends after this elapsed time even if active.

        timezone : typing.Optional[CreateAgentRequestTimezone]
            Timezone applied to scheduled actions and timestamps the agent reports to the user.

        call_disposition_config : typing.Optional[str]
            Configuration string for call disposition tracking.

        allow_inbound_call : typing.Optional[bool]
            Whether the agent accepts inbound calls.

        enable_style_guide : typing.Optional[bool]
            Whether style guide enforcement is applied to agent responses.

        speech_formatting : typing.Optional[bool]
            Whether speech formatting is applied to the agent's responses.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateAgentAgentsResponse
            Agent created successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atoms.agents.create_agent(
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_agent(
            name=name,
            description=description,
            background_sound=background_sound,
            language=language,
            synthesizer=synthesizer,
            global_knowledge_base_id=global_knowledge_base_id,
            slm_model=slm_model,
            default_variables=default_variables,
            pre_call_api=pre_call_api,
            global_prompt=global_prompt,
            telephony_product_id=telephony_product_id,
            workflow_type=workflow_type,
            first_message=first_message,
            mute_user_until_first_bot_response=mute_user_until_first_bot_response,
            allow_interruptions=allow_interruptions,
            wait_for_user_to_speak_first=wait_for_user_to_speak_first,
            interruption_backoff_timer=interruption_backoff_timer,
            smart_turn_config=smart_turn_config,
            voice_detection_config=voice_detection_config,
            voice_mail_detection_config=voice_mail_detection_config,
            denoising_config=denoising_config,
            redaction_config=redaction_config,
            pronunciation_dicts=pronunciation_dicts,
            llm_idle_timeout_config=llm_idle_timeout_config,
            session_timeout_config=session_timeout_config,
            timezone=timezone,
            call_disposition_config=call_disposition_config,
            allow_inbound_call=allow_inbound_call,
            enable_style_guide=enable_style_guide,
            speech_formatting=speech_formatting,
            request_options=request_options,
        )
        return _response.data

    async def get_agent(
        self,
        id: str,
        *,
        draft_id: typing.Optional[str] = None,
        version_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetAgentAgentsResponse:
        """
        Returns the agent document merged with the resolved config of the active version
        under `_resolvedConfig`. Non-versioned fields (name, telephonyProductId, allowInboundCall,
        etc.) sit at the top level; versioned fields (prompt, tools, language, synthesizer,
        post-call analytics, …) are resolved from the target version and exposed under `_resolvedConfig`.

        **Previewing a draft or specific version**

        Pass `?draftId=<id>` to resolve config from a specific draft instead of the active version.
        Pass `?versionId=<id>` to resolve config from a specific published version.
        When either param is used, the response includes `_configSource: "draft" | "version" | "active"`
        indicating which config was resolved.

        Notable resolved fields in `_resolvedConfig`:

        - `prompt` — active version's single-prompt text
        - `tools` — configured tools on the resolved version
        - `postCallAnalyticsConfig` — disposition metrics + analytics model flags
        - `modelName` — LLM model name on the resolved version
        - `defaultLanguage`, `supportedLanguages` — active language config
        - `firstMessage`, `globalPrompt` — active messaging config
        - `workflowGraph` — full node graph for `workflow_graph` agents

        To read prompt + tools alone, use `GET /agent/{id}/workflow` (deprecated for
        new integrations but still live). To inspect a specific non-active version,
        use `GET /agent/{id}/versions/{versionId}`.

        **400 — also used for "not found":** if the agent ID does not exist in the
        organization, the API returns 400 with `errors: ["No agent found"]` rather than 404.

        Parameters
        ----------
        id : str

        draft_id : typing.Optional[str]
            Resolve `_resolvedConfig` from this draft instead of the active version. Sets `_configSource` to `"draft"` in the response.

        version_id : typing.Optional[str]
            Resolve `_resolvedConfig` from this published version instead of the active version. Sets `_configSource` to `"version"` in the response.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentAgentsResponse
            Successful response

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atoms.agents.get_agent(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_agent(
            id, draft_id=draft_id, version_id=version_id, request_options=request_options
        )
        return _response.data

    async def update_agent(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        avatar_url: typing.Optional[str] = OMIT,
        telephony_product_id: typing.Optional[typing.Sequence[str]] = OMIT,
        allow_inbound_call: typing.Optional[bool] = OMIT,
        visible_to_everyone: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateAgentAgentsResponse:
        """
        Update agent fields. Behavior depends on whether the agent has versioning enabled:

        **Versioned agents** (have an active published version): only metadata fields are accepted —
        `name`, `description`, `avatarUrl`, `telephonyProductId`, `allowInboundCall`, `visibleToEveryone`.
        Submitting any config-level field returns 400 with
        `"Agent has versioning enabled. Config changes must be made through drafts."`.
        Use `PATCH /agent/{id}/drafts/{draftId}/config` instead.

        **Non-versioned agents** (no active version): all configuration fields are accepted,
        the same full set as `POST /agent`.

        **400 is also returned when:**
        - The agent is locked (`"Agent is locked, please unlock it to update"`)
        - Cross-field constraint violated (e.g. `north_indic` language requires `transcriberType: pulse`)

        **403** is returned when selecting a gated model (`gpt-5.2`, `electron-kogta`, `electron-kogta-v2`)
        without org-level access.

        Parameters
        ----------
        id : str

        name : typing.Optional[str]
            Name of the agent.

        description : typing.Optional[str]
            Description of the agent.

        avatar_url : typing.Optional[str]
            URL of the agent's avatar image.

        telephony_product_id : typing.Optional[typing.Sequence[str]]
            IDs of telephony products (phone numbers) to associate with the agent.

        allow_inbound_call : typing.Optional[bool]
            Whether the agent accepts inbound calls.

        visible_to_everyone : typing.Optional[bool]
            Whether the agent is visible to all members of the organization.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateAgentAgentsResponse
            Agent updated successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atoms.agents.update_agent(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_agent(
            id,
            name=name,
            description=description,
            avatar_url=avatar_url,
            telephony_product_id=telephony_product_id,
            allow_inbound_call=allow_inbound_call,
            visible_to_everyone=visible_to_everyone,
            request_options=request_options,
        )
        return _response.data

    async def archive_agent(
        self, id: str, *, on: typing.Optional[bool] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> ArchiveAgentAgentsResponse:
        """
        Soft-archives the agent — it is excluded from listings and stops accepting calls,
        but all data is preserved and the operation is fully reversible.

        Pass `?on=false` to unarchive (restore) a previously archived agent.

        **409 is returned when:**
        - The agent is already in the requested state (`"Agent is already archived"` / `"Agent is already active"`)
        - The agent is linked to an active campaign (`"Agent is associated with the [status] campaign "[name]". Complete or remove the campaign before archiving."`)

        Parameters
        ----------
        id : str

        on : typing.Optional[bool]
            `true` (default) — archive the agent.
            `false` — unarchive (restore) a previously archived agent.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ArchiveAgentAgentsResponse
            Agent archived or unarchived successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atoms.agents.archive_agent(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.archive_agent(id, on=on, request_options=request_options)
        return _response.data

    async def duplicate_agent(
        self, id: str, *, target_organization_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> DuplicateAgentAgentsResponse:
        """
        Duplicates a SINGLE_PROMPT agent's live active version into a target organization
        (can also be the same organization). Copies all versioned configuration but strips
        organization-specific resources: knowledge base tools are removed, default variable
        values are blanked, and a new avatar is generated. The duplicate starts with a
        published V1 as its active version.

        **400 is returned when:**
        - The source agent is archived (`"Cannot duplicate an archived agent"`)
        - The agent has no `activeVersionId` (`"This agent has no active version and cannot be duplicated"`)
        - The active version exists but is not published/active (`"This agent has no active published version and cannot be duplicated"`)
        - The agent is not `SINGLE_PROMPT` workflow type

        Parameters
        ----------
        id : str
            The ID of the source agent to duplicate

        target_organization_id : str
            MongoDB ObjectId of the target organization. Must be a 24-character hex string.
            The authenticated user must be a member of this organization.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DuplicateAgentAgentsResponse
            Agent duplicated successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atoms.agents.duplicate_agent(
                id="id",
                target_organization_id="60d0fe4f5311236168a109ca",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.duplicate_agent(
            id, target_organization_id=target_organization_id, request_options=request_options
        )
        return _response.data

    async def get_agent_workflow(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GetAgentIdWorkflowResponse:
        """
        **Deprecated** — prefer `GET /agent/{id}` (config is resolved into `_resolvedConfig`
        including prompt, tools, and post-call analytics).

        Returns the active version's prompt and tools for single-prompt agents, or the
        workflow graph data for workflow_graph agents. Customers still rely on this to
        fetch their current prompt + tools — endpoint is kept live for now.

        **Caveat:** the `versionId` query param (if passed) is silently ignored.
        The response always reflects the currently-active version. To inspect a
        non-active version, use `GET /agent/{id}/versions/{versionId}`.

        Parameters
        ----------
        id : str
            The ID of the agent

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentIdWorkflowResponse
            Successful response

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atoms.agents.get_agent_workflow(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_agent_workflow(id, request_options=request_options)
        return _response.data

    async def update_workflow_configuration(
        self,
        id: str,
        *,
        type: WorkflowType,
        workflow_graph: typing.Optional[UpdateWorkflowConfigurationAgentsRequestWorkflowGraph] = OMIT,
        single_prompt_config: typing.Optional[SinglePromptConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        **Deprecated** — use `PATCH /agent/{id}/drafts/{draftId}/config` instead.

        Directly mutates the legacy workflow document for an agent. This write path
        bypasses the versioning system entirely: the change is not captured as a
        new version, and future version activations may overwrite the legacy doc
        back to whatever the version snapshot contains.

        ⚠ **Writing here on a versioned agent can silently wipe tools, prompt, or
        other fields that were missing from the PATCH payload.** Only use this if
        you know the agent is not using versioning, or if you are intentionally
        hot-patching the legacy doc.

        Parameters
        ----------
        id : str
            The workflow ID (found at `agent.workflowId` on the agent document).

        type : WorkflowType

        workflow_graph : typing.Optional[UpdateWorkflowConfigurationAgentsRequestWorkflowGraph]
            Required when `type = workflow_graph`. Exactly one of `workflowGraph` or `singlePromptConfig` must be provided.

        single_prompt_config : typing.Optional[SinglePromptConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.atoms.agents.update_workflow_configuration(
                id="60d0fe4f5311236168a109ca",
                type="workflow_graph",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_workflow_configuration(
            id,
            type=type,
            workflow_graph=workflow_graph,
            single_prompt_config=single_prompt_config,
            request_options=request_options,
        )
        return _response.data
