# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ...core.pydantic_utilities import IS_PYDANTIC_V2
from ...core.unchecked_base_model import UncheckedBaseModel
from .registry_tool_definition_auth_location import RegistryToolDefinitionAuthLocation
from .registry_tool_definition_auth_type import RegistryToolDefinitionAuthType


class RegistryToolDefinitionAuth(UncheckedBaseModel):
    """
    Optional for `api_call` type. Authentication for the outbound request.
    Credentials are referenced **by secret name** (from the org Secrets vault,
    see `POST /secret`), never inline. At call time the platform decrypts the
    secret, injects it into the request, and strips the `auth` block before the
    config reaches the runtime, cache, or webhooks. `token`, `value`, and
    `password` below are secret names, not literal values.
    """

    type: RegistryToolDefinitionAuthType = pydantic.Field()
    """
    Auth scheme. `bearer` sends `Authorization: Bearer <secret>`; `api_key`
    sends the secret in a header or query param you name; `basic` sends
    `Authorization: Basic <base64(username:secret)>`.
    """

    token: typing.Optional[str] = pydantic.Field(default=None)
    """
    For `bearer`: the name of the secret holding the token.
    """

    name: typing.Optional[str] = pydantic.Field(default=None)
    """
    For `api_key`: the header or query-param name to send the key under.
    """

    location: typing.Optional[RegistryToolDefinitionAuthLocation] = pydantic.Field(default=None)
    """
    For `api_key`: whether the key is sent as a header or a query parameter.
    """

    value: typing.Optional[str] = pydantic.Field(default=None)
    """
    For `api_key`: the name of the secret holding the key.
    """

    username: typing.Optional[str] = pydantic.Field(default=None)
    """
    For `basic`: the name of the secret holding the username.
    """

    password: typing.Optional[str] = pydantic.Field(default=None)
    """
    For `basic`: the name of the secret holding the password.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
