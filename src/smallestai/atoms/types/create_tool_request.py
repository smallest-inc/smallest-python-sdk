# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ...core.pydantic_utilities import IS_PYDANTIC_V2
from ...core.unchecked_base_model import UncheckedBaseModel
from .registry_tool_definition import RegistryToolDefinition


class CreateToolRequest(UncheckedBaseModel):
    definition: RegistryToolDefinition = pydantic.Field()
    """
    The tool body — the same shape used inside an agent's `tools[]`
    (discriminated on `type`). `api_call` is the most common; `client_tool`
    and the built-in types are also valid.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
