# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from .raw_client import AsyncRawPromptScoringClient, RawPromptScoringClient
from .types.post_prompt_scoring_score_request import PostPromptScoringScoreRequest
from .types.post_prompt_scoring_score_response import PostPromptScoringScoreResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class PromptScoringClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPromptScoringClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPromptScoringClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPromptScoringClient
        """
        return self._raw_client

    def score_a_prompt(
        self, *, request: PostPromptScoringScoreRequest, request_options: typing.Optional[RequestOptions] = None
    ) -> PostPromptScoringScoreResponse:
        """
        Scores an agent's prompt across 11 quality dimensions using Gemini-based analysis. Requires the prompt to have changed since the last scoring.

        **Input:** Provide exactly one of `versionId` (published agent version) or `draftId` (agent draft). Providing both or neither returns a 400.

        **Credit usage:** 1 credit is deducted per successful call.

        **Idempotency:** Re-submitting the same prompt without changes returns a 400 — retrieve the cached score via the GET agent endpoint instead.

        **Supported agent types:** Only `single_prompt` agents are supported. Workflow-graph agents return a 400.

        **Scoring model:** Two sequential Gemini calls — a Platform Analyst pass followed by a Rubric Judge pass.

        ### Scored Dimensions

        | Tier | Dimension | Notes |
        |------|-----------|-------|
        | 1 | Role & Objective | |
        | 1 | Personality & Voice | |
        | 1 | Conversation Structure | |
        | 1 | Tool Integration | |
        | 1 | Constraints & Safety | |
        | 2 | Conversational Naturalness | |
        | 2 | Failure-Mode Coverage | |
        | 3 | Information Integrity | Gating — if Weak/Missing, score capped at 70 |
        | 3 | Variable & Tool Hygiene | Gating — if Weak/Missing, score capped at 50 |
        | 3 | Internal Consistency | |
        | 3 | Density | Computed from token analysis |

        Parameters
        ----------
        request : PostPromptScoringScoreRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PostPromptScoringScoreResponse
            Prompt scored successfully

        Examples
        --------
        from smallestai import SmallestAI
        from smallestai.atoms.prompt_scoring import (
            PostPromptScoringScoreRequestVersionId,
        )

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.prompt_scoring.score_a_prompt(
            request=PostPromptScoringScoreRequestVersionId(
                version_id="6a1589b75e048394eb37bc47",
            ),
        )
        """
        _response = self._raw_client.score_a_prompt(request=request, request_options=request_options)
        return _response.data


class AsyncPromptScoringClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPromptScoringClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPromptScoringClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPromptScoringClient
        """
        return self._raw_client

    async def score_a_prompt(
        self, *, request: PostPromptScoringScoreRequest, request_options: typing.Optional[RequestOptions] = None
    ) -> PostPromptScoringScoreResponse:
        """
        Scores an agent's prompt across 11 quality dimensions using Gemini-based analysis. Requires the prompt to have changed since the last scoring.

        **Input:** Provide exactly one of `versionId` (published agent version) or `draftId` (agent draft). Providing both or neither returns a 400.

        **Credit usage:** 1 credit is deducted per successful call.

        **Idempotency:** Re-submitting the same prompt without changes returns a 400 — retrieve the cached score via the GET agent endpoint instead.

        **Supported agent types:** Only `single_prompt` agents are supported. Workflow-graph agents return a 400.

        **Scoring model:** Two sequential Gemini calls — a Platform Analyst pass followed by a Rubric Judge pass.

        ### Scored Dimensions

        | Tier | Dimension | Notes |
        |------|-----------|-------|
        | 1 | Role & Objective | |
        | 1 | Personality & Voice | |
        | 1 | Conversation Structure | |
        | 1 | Tool Integration | |
        | 1 | Constraints & Safety | |
        | 2 | Conversational Naturalness | |
        | 2 | Failure-Mode Coverage | |
        | 3 | Information Integrity | Gating — if Weak/Missing, score capped at 70 |
        | 3 | Variable & Tool Hygiene | Gating — if Weak/Missing, score capped at 50 |
        | 3 | Internal Consistency | |
        | 3 | Density | Computed from token analysis |

        Parameters
        ----------
        request : PostPromptScoringScoreRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PostPromptScoringScoreResponse
            Prompt scored successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI
        from smallestai.atoms.prompt_scoring import (
            PostPromptScoringScoreRequestVersionId,
        )

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.prompt_scoring.score_a_prompt(
                request=PostPromptScoringScoreRequestVersionId(
                    version_id="6a1589b75e048394eb37bc47",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.score_a_prompt(request=request, request_options=request_options)
        return _response.data
