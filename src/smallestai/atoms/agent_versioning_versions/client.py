# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from ..types.test_call_request_mode import TestCallRequestMode
from .raw_client import AsyncRawAgentVersioningVersionsClient, RawAgentVersioningVersionsClient
from .types.activate_version_agent_versioning_versions_response import ActivateVersionAgentVersioningVersionsResponse
from .types.compare_version_metrics_agent_versioning_versions_response import (
    CompareVersionMetricsAgentVersioningVersionsResponse,
)
from .types.get_agent_id_versions_diff_response import GetAgentIdVersionsDiffResponse
from .types.get_agent_id_versions_response import GetAgentIdVersionsResponse
from .types.get_agent_id_versions_version_id_response import GetAgentIdVersionsVersionIdResponse
from .types.post_agent_id_versions_version_id_test_call_response import PostAgentIdVersionsVersionIdTestCallResponse
from .types.update_version_metadata_agent_versioning_versions_response import (
    UpdateVersionMetadataAgentVersioningVersionsResponse,
)

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AgentVersioningVersionsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAgentVersioningVersionsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAgentVersioningVersionsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAgentVersioningVersionsClient
        """
        return self._raw_client

    def list_published_versions(
        self,
        id: str,
        *,
        limit: typing.Optional[int] = None,
        skip: typing.Optional[int] = None,
        is_pinned: typing.Optional[bool] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetAgentIdVersionsResponse:
        """
        List published versions for an agent with pagination and optional pin filter.
        The `total` value currently represents the total number of published versions
        for the agent, not necessarily the filtered count when `isPinned` is used.

        Parameters
        ----------
        id : str
            The agent ID

        limit : typing.Optional[int]
            Number of versions to return (1-100, default 20)

        skip : typing.Optional[int]
            Number of versions to skip (default 0)

        is_pinned : typing.Optional[bool]
            Filter by pinned status

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentIdVersionsResponse
            Successful response

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.agent_versioning_versions.list_published_versions(
            id="60d0fe4f5311236168a109ca",
        )
        """
        _response = self._raw_client.list_published_versions(
            id, limit=limit, skip=skip, is_pinned=is_pinned, request_options=request_options
        )
        return _response.data

    def diff_two_versions(
        self, id: str, *, version_a: str, version_b: str, request_options: typing.Optional[RequestOptions] = None
    ) -> GetAgentIdVersionsDiffResponse:
        """
        Compare two version or draft revision records side-by-side by their IDs. The implementation tries published versions first and can fall back to the latest draft revision.

        Parameters
        ----------
        id : str
            The agent ID

        version_a : str
            ID of the first version

        version_b : str
            ID of the second version

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentIdVersionsDiffResponse
            Diff returned successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.agent_versioning_versions.diff_two_versions(
            id="60d0fe4f5311236168a109ca",
            version_a="versionA",
            version_b="versionB",
        )
        """
        _response = self._raw_client.diff_two_versions(
            id, version_a=version_a, version_b=version_b, request_options=request_options
        )
        return _response.data

    def compare_version_metrics(
        self,
        id: str,
        *,
        version_a: str,
        version_b: str,
        date_from: typing.Optional[dt.date] = None,
        date_to: typing.Optional[dt.date] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CompareVersionMetricsAgentVersioningVersionsResponse:
        """
        Compare analytics/call metrics between two published versions over an optional date range.

        Parameters
        ----------
        id : str
            The agent ID

        version_a : str
            ID of the first version

        version_b : str
            ID of the second version

        date_from : typing.Optional[dt.date]
            Start date for the comparison range in YYYY-MM-DD format.

        date_to : typing.Optional[dt.date]
            End date for the comparison range in YYYY-MM-DD format.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CompareVersionMetricsAgentVersioningVersionsResponse
            Metrics comparison returned

        Examples
        --------
        import datetime

        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.agent_versioning_versions.compare_version_metrics(
            id="60d0fe4f5311236168a109ca",
            version_a="versionA",
            version_b="versionB",
            date_from=datetime.date.fromisoformat(
                "2026-05-01",
            ),
            date_to=datetime.date.fromisoformat(
                "2026-05-31",
            ),
        )
        """
        _response = self._raw_client.compare_version_metrics(
            id,
            version_a=version_a,
            version_b=version_b,
            date_from=date_from,
            date_to=date_to,
            request_options=request_options,
        )
        return _response.data

    def get_version_detail(
        self, id: str, version_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GetAgentIdVersionsVersionIdResponse:
        """
        Returns the full detail of a specific published version (read-only).
        Published versions are config-immutable — to modify config, create a draft from
        this version and publish it as a new version.

        Parameters
        ----------
        id : str
            The agent ID

        version_id : str
            The published version ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentIdVersionsVersionIdResponse
            Successful response

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.agent_versioning_versions.get_version_detail(
            id="60d0fe4f5311236168a109ca",
            version_id="versionId",
        )
        """
        _response = self._raw_client.get_version_detail(id, version_id, request_options=request_options)
        return _response.data

    def update_version_metadata(
        self,
        id: str,
        version_id: str,
        *,
        label: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        is_pinned: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateVersionMetadataAgentVersioningVersionsResponse:
        """
        Update a published version's label, description, or pinned status. At least one field is required.
        Published versions (both active and inactive) are config-immutable — their agent
        configuration cannot be changed. To modify config, create a new draft from the version,
        edit the draft, and publish it as a new version.

        Parameters
        ----------
        id : str
            The agent ID

        version_id : str
            The published version ID

        label : typing.Optional[str]
            Version label

        description : typing.Optional[str]
            Version description

        is_pinned : typing.Optional[bool]
            Pin or unpin the version

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateVersionMetadataAgentVersioningVersionsResponse
            Version metadata updated

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.agent_versioning_versions.update_version_metadata(
            id="60d0fe4f5311236168a109ca",
            version_id="versionId",
        )
        """
        _response = self._raw_client.update_version_metadata(
            id, version_id, label=label, description=description, is_pinned=is_pinned, request_options=request_options
        )
        return _response.data

    def activate_version(
        self, id: str, version_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ActivateVersionAgentVersioningVersionsResponse:
        """
        Set a published version as the active version for the agent. The previously
        active version is deactivated. This does not modify the version's config — it
        only changes which version serves live traffic. Activation is idempotent: if
        the version is already active, the endpoint returns that version without
        changing config.

        Parameters
        ----------
        id : str
            The agent ID

        version_id : str
            The published version ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ActivateVersionAgentVersioningVersionsResponse
            Version activated successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.agent_versioning_versions.activate_version(
            id="60d0fe4f5311236168a109ca",
            version_id="versionId",
        )
        """
        _response = self._raw_client.activate_version(id, version_id, request_options=request_options)
        return _response.data

    def test_call_with_version_config(
        self,
        id: str,
        version_id: str,
        *,
        mode: typing.Optional[TestCallRequestMode] = OMIT,
        to_phone: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PostAgentIdVersionsVersionIdTestCallResponse:
        """
        Initiate a test call using a specific published version's configuration.
        The response always includes `conversationId` and `callId`. For `webcall`
        and `chat`, it also includes `token`, `roomName`, and `host`. Those fields
        are omitted for `telephony`.

        Parameters
        ----------
        id : str
            The agent ID

        version_id : str
            The published version ID

        mode : typing.Optional[TestCallRequestMode]
            Test call mode. Defaults to `webcall` when omitted.

        to_phone : typing.Optional[str]
            Phone number to call. Required only when `mode` is `telephony`. Omit for `webcall` and `chat`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PostAgentIdVersionsVersionIdTestCallResponse
            Test call initiated

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.agent_versioning_versions.test_call_with_version_config(
            id="60d0fe4f5311236168a109ca",
            version_id="versionId",
        )
        """
        _response = self._raw_client.test_call_with_version_config(
            id, version_id, mode=mode, to_phone=to_phone, request_options=request_options
        )
        return _response.data


class AsyncAgentVersioningVersionsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAgentVersioningVersionsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAgentVersioningVersionsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAgentVersioningVersionsClient
        """
        return self._raw_client

    async def list_published_versions(
        self,
        id: str,
        *,
        limit: typing.Optional[int] = None,
        skip: typing.Optional[int] = None,
        is_pinned: typing.Optional[bool] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetAgentIdVersionsResponse:
        """
        List published versions for an agent with pagination and optional pin filter.
        The `total` value currently represents the total number of published versions
        for the agent, not necessarily the filtered count when `isPinned` is used.

        Parameters
        ----------
        id : str
            The agent ID

        limit : typing.Optional[int]
            Number of versions to return (1-100, default 20)

        skip : typing.Optional[int]
            Number of versions to skip (default 0)

        is_pinned : typing.Optional[bool]
            Filter by pinned status

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentIdVersionsResponse
            Successful response

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.agent_versioning_versions.list_published_versions(
                id="60d0fe4f5311236168a109ca",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_published_versions(
            id, limit=limit, skip=skip, is_pinned=is_pinned, request_options=request_options
        )
        return _response.data

    async def diff_two_versions(
        self, id: str, *, version_a: str, version_b: str, request_options: typing.Optional[RequestOptions] = None
    ) -> GetAgentIdVersionsDiffResponse:
        """
        Compare two version or draft revision records side-by-side by their IDs. The implementation tries published versions first and can fall back to the latest draft revision.

        Parameters
        ----------
        id : str
            The agent ID

        version_a : str
            ID of the first version

        version_b : str
            ID of the second version

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentIdVersionsDiffResponse
            Diff returned successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.agent_versioning_versions.diff_two_versions(
                id="60d0fe4f5311236168a109ca",
                version_a="versionA",
                version_b="versionB",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.diff_two_versions(
            id, version_a=version_a, version_b=version_b, request_options=request_options
        )
        return _response.data

    async def compare_version_metrics(
        self,
        id: str,
        *,
        version_a: str,
        version_b: str,
        date_from: typing.Optional[dt.date] = None,
        date_to: typing.Optional[dt.date] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CompareVersionMetricsAgentVersioningVersionsResponse:
        """
        Compare analytics/call metrics between two published versions over an optional date range.

        Parameters
        ----------
        id : str
            The agent ID

        version_a : str
            ID of the first version

        version_b : str
            ID of the second version

        date_from : typing.Optional[dt.date]
            Start date for the comparison range in YYYY-MM-DD format.

        date_to : typing.Optional[dt.date]
            End date for the comparison range in YYYY-MM-DD format.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CompareVersionMetricsAgentVersioningVersionsResponse
            Metrics comparison returned

        Examples
        --------
        import asyncio
        import datetime

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.agent_versioning_versions.compare_version_metrics(
                id="60d0fe4f5311236168a109ca",
                version_a="versionA",
                version_b="versionB",
                date_from=datetime.date.fromisoformat(
                    "2026-05-01",
                ),
                date_to=datetime.date.fromisoformat(
                    "2026-05-31",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.compare_version_metrics(
            id,
            version_a=version_a,
            version_b=version_b,
            date_from=date_from,
            date_to=date_to,
            request_options=request_options,
        )
        return _response.data

    async def get_version_detail(
        self, id: str, version_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GetAgentIdVersionsVersionIdResponse:
        """
        Returns the full detail of a specific published version (read-only).
        Published versions are config-immutable — to modify config, create a draft from
        this version and publish it as a new version.

        Parameters
        ----------
        id : str
            The agent ID

        version_id : str
            The published version ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentIdVersionsVersionIdResponse
            Successful response

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.agent_versioning_versions.get_version_detail(
                id="60d0fe4f5311236168a109ca",
                version_id="versionId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_version_detail(id, version_id, request_options=request_options)
        return _response.data

    async def update_version_metadata(
        self,
        id: str,
        version_id: str,
        *,
        label: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        is_pinned: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateVersionMetadataAgentVersioningVersionsResponse:
        """
        Update a published version's label, description, or pinned status. At least one field is required.
        Published versions (both active and inactive) are config-immutable — their agent
        configuration cannot be changed. To modify config, create a new draft from the version,
        edit the draft, and publish it as a new version.

        Parameters
        ----------
        id : str
            The agent ID

        version_id : str
            The published version ID

        label : typing.Optional[str]
            Version label

        description : typing.Optional[str]
            Version description

        is_pinned : typing.Optional[bool]
            Pin or unpin the version

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateVersionMetadataAgentVersioningVersionsResponse
            Version metadata updated

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.agent_versioning_versions.update_version_metadata(
                id="60d0fe4f5311236168a109ca",
                version_id="versionId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_version_metadata(
            id, version_id, label=label, description=description, is_pinned=is_pinned, request_options=request_options
        )
        return _response.data

    async def activate_version(
        self, id: str, version_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ActivateVersionAgentVersioningVersionsResponse:
        """
        Set a published version as the active version for the agent. The previously
        active version is deactivated. This does not modify the version's config — it
        only changes which version serves live traffic. Activation is idempotent: if
        the version is already active, the endpoint returns that version without
        changing config.

        Parameters
        ----------
        id : str
            The agent ID

        version_id : str
            The published version ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ActivateVersionAgentVersioningVersionsResponse
            Version activated successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.agent_versioning_versions.activate_version(
                id="60d0fe4f5311236168a109ca",
                version_id="versionId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.activate_version(id, version_id, request_options=request_options)
        return _response.data

    async def test_call_with_version_config(
        self,
        id: str,
        version_id: str,
        *,
        mode: typing.Optional[TestCallRequestMode] = OMIT,
        to_phone: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PostAgentIdVersionsVersionIdTestCallResponse:
        """
        Initiate a test call using a specific published version's configuration.
        The response always includes `conversationId` and `callId`. For `webcall`
        and `chat`, it also includes `token`, `roomName`, and `host`. Those fields
        are omitted for `telephony`.

        Parameters
        ----------
        id : str
            The agent ID

        version_id : str
            The published version ID

        mode : typing.Optional[TestCallRequestMode]
            Test call mode. Defaults to `webcall` when omitted.

        to_phone : typing.Optional[str]
            Phone number to call. Required only when `mode` is `telephony`. Omit for `webcall` and `chat`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PostAgentIdVersionsVersionIdTestCallResponse
            Test call initiated

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.agent_versioning_versions.test_call_with_version_config(
                id="60d0fe4f5311236168a109ca",
                version_id="versionId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.test_call_with_version_config(
            id, version_id, mode=mode, to_phone=to_phone, request_options=request_options
        )
        return _response.data
