# This file was auto-generated by Fern from our API Definition.

import typing

from ... import core
from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from .raw_client import AsyncRawComplianceClient, RawComplianceClient
from .types.get_compliance_requirements_request_number_type import GetComplianceRequirementsRequestNumberType
from .types.get_compliance_requirements_request_user_type import GetComplianceRequirementsRequestUserType
from .types.get_compliance_requirements_response import GetComplianceRequirementsResponse
from .types.get_compliance_status_request_number_type import GetComplianceStatusRequestNumberType
from .types.get_compliance_status_request_user_type import GetComplianceStatusRequestUserType
from .types.get_compliance_status_response import GetComplianceStatusResponse
from .types.post_compliance_applications_id_refresh_response import PostComplianceApplicationsIdRefreshResponse
from .types.resubmit_compliance_response import ResubmitComplianceResponse
from .types.submit_compliance_request_number_type import SubmitComplianceRequestNumberType
from .types.submit_compliance_request_user_type import SubmitComplianceRequestUserType
from .types.submit_compliance_response import SubmitComplianceResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ComplianceClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawComplianceClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawComplianceClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawComplianceClient
        """
        return self._raw_client

    def get_compliance_status(
        self,
        *,
        country_iso: str,
        number_type: GetComplianceStatusRequestNumberType,
        user_type: GetComplianceStatusRequestUserType,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetComplianceStatusResponse:
        """
        Returns the current compliance status for a given country, number type, and user type.
        This is the single endpoint the frontend uses to determine which step to render
        (form, submitted, accepted, rejected, expired, or suspended).

        Parameters
        ----------
        country_iso : str
            ISO 3166-1 alpha-2 country code. Must be exactly 2 characters (e.g. "IN", "US"). Sending 3+ characters returns 400.

        number_type : GetComplianceStatusRequestNumberType
            The type of phone number

        user_type : GetComplianceStatusRequestUserType
            The type of end user

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetComplianceStatusResponse
            Compliance status retrieved successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.compliance.get_compliance_status(
            country_iso="IN",
            number_type="local",
            user_type="individual",
        )
        """
        _response = self._raw_client.get_compliance_status(
            country_iso=country_iso, number_type=number_type, user_type=user_type, request_options=request_options
        )
        return _response.data

    def get_compliance_requirements(
        self,
        *,
        country_iso: str,
        number_type: GetComplianceRequirementsRequestNumberType,
        user_type: GetComplianceRequirementsRequestUserType,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetComplianceRequirementsResponse:
        """
        Discover what documents are required for a given country, number type, and user type.
        Results are cached for 1 hour. Returns an empty `documentTypes` array if no compliance
        is needed for the given combination.

        Parameters
        ----------
        country_iso : str
            ISO 3166-1 alpha-2 country code. Must be exactly 2 characters (e.g. "IN", "US"). Sending 3+ characters returns 400.

        number_type : GetComplianceRequirementsRequestNumberType
            The type of phone number

        user_type : GetComplianceRequirementsRequestUserType
            The type of end user

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetComplianceRequirementsResponse
            Requirements retrieved successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.compliance.get_compliance_requirements(
            country_iso="IN",
            number_type="local",
            user_type="individual",
        )
        """
        _response = self._raw_client.get_compliance_requirements(
            country_iso=country_iso, number_type=number_type, user_type=user_type, request_options=request_options
        )
        return _response.data

    def submit(
        self,
        *,
        country_iso: str,
        number_type: SubmitComplianceRequestNumberType,
        user_type: SubmitComplianceRequestUserType,
        end_user: str,
        documents: str,
        files: typing.List[core.File],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitComplianceResponse:
        """
        Submit a new compliance application with end-user details and supporting documents.
        One application is allowed per organization per country per number type per user type.

        The request uses `multipart/form-data` because documents are uploaded inline.
        The `endUser` and `documents` fields are JSON strings embedded in the form data.

        Parameters
        ----------
        country_iso : str
            ISO 3166-1 alpha-2 country code

        number_type : SubmitComplianceRequestNumberType
            The type of phone number

        user_type : SubmitComplianceRequestUserType
            The type of end user

        end_user : str
            JSON-stringified end-user details. `name` is required; all other fields are optional
            but may be required by Plivo depending on country/numberType. Accepted fields:

            - `name` (required) — full name or business name
            - `lastName` — last name
            - `email` — email address
            - `addressLine1` — street address line 1
            - `addressLine2` — street address line 2
            - `city` — city
            - `state` — state or province
            - `postalCode` — postal/ZIP code
            - `country` — ISO country code; defaults to `countryIso` if omitted
            - `registrationNumber` — business registration number (required for some business applications)

        documents : str
            JSON string containing an array of document metadata. Each entry must have a
            `documentTypeId` (from the requirements endpoint) and optional `dataFields`.
            Example:
            ```json
            [{"documentTypeId": "dt_123", "dataFields": {"business_name": "Acme Corp"}}]
            ```

        files : typing.List[core.File]
            See core.File for more documentation

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitComplianceResponse
            Application submitted successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.compliance.submit(
            country_iso="countryIso",
            number_type="local",
            user_type="individual",
            end_user="endUser",
            documents="documents",
        )
        """
        _response = self._raw_client.submit(
            country_iso=country_iso,
            number_type=number_type,
            user_type=user_type,
            end_user=end_user,
            documents=documents,
            files=files,
            request_options=request_options,
        )
        return _response.data

    def resubmit(
        self,
        id: str,
        *,
        documents: str,
        files: typing.List[core.File],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResubmitComplianceResponse:
        """
        Resubmit a previously rejected compliance application with corrected documents.
        Only applications in `rejected` status can be resubmitted. All documents must be
        re-uploaded — partial updates are not supported.

        File/document count must match exactly. Mismatch returns 400 with message `"Expected X files, got Y"`.

        Parameters
        ----------
        id : str
            The compliance application ID

        documents : str
            JSON string containing an array of document metadata. Same format as
            the create endpoint.

        files : typing.List[core.File]
            See core.File for more documentation

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResubmitComplianceResponse
            Application resubmitted successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.compliance.resubmit(
            id="id",
            documents="documents",
        )
        """
        _response = self._raw_client.resubmit(id, documents=documents, files=files, request_options=request_options)
        return _response.data

    def refresh_compliance_application_status(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PostComplianceApplicationsIdRefreshResponse:
        """
        Manually poll Plivo for the latest status of a compliance application.
        Use this as a fallback when webhooks are delayed. The frontend enforces a
        60-second cooldown between refreshes.

        Parameters
        ----------
        id : str
            The compliance application ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PostComplianceApplicationsIdRefreshResponse
            Application status refreshed

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.compliance.refresh_compliance_application_status(
            id="id",
        )
        """
        _response = self._raw_client.refresh_compliance_application_status(id, request_options=request_options)
        return _response.data


class AsyncComplianceClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawComplianceClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawComplianceClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawComplianceClient
        """
        return self._raw_client

    async def get_compliance_status(
        self,
        *,
        country_iso: str,
        number_type: GetComplianceStatusRequestNumberType,
        user_type: GetComplianceStatusRequestUserType,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetComplianceStatusResponse:
        """
        Returns the current compliance status for a given country, number type, and user type.
        This is the single endpoint the frontend uses to determine which step to render
        (form, submitted, accepted, rejected, expired, or suspended).

        Parameters
        ----------
        country_iso : str
            ISO 3166-1 alpha-2 country code. Must be exactly 2 characters (e.g. "IN", "US"). Sending 3+ characters returns 400.

        number_type : GetComplianceStatusRequestNumberType
            The type of phone number

        user_type : GetComplianceStatusRequestUserType
            The type of end user

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetComplianceStatusResponse
            Compliance status retrieved successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.compliance.get_compliance_status(
                country_iso="IN",
                number_type="local",
                user_type="individual",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_compliance_status(
            country_iso=country_iso, number_type=number_type, user_type=user_type, request_options=request_options
        )
        return _response.data

    async def get_compliance_requirements(
        self,
        *,
        country_iso: str,
        number_type: GetComplianceRequirementsRequestNumberType,
        user_type: GetComplianceRequirementsRequestUserType,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetComplianceRequirementsResponse:
        """
        Discover what documents are required for a given country, number type, and user type.
        Results are cached for 1 hour. Returns an empty `documentTypes` array if no compliance
        is needed for the given combination.

        Parameters
        ----------
        country_iso : str
            ISO 3166-1 alpha-2 country code. Must be exactly 2 characters (e.g. "IN", "US"). Sending 3+ characters returns 400.

        number_type : GetComplianceRequirementsRequestNumberType
            The type of phone number

        user_type : GetComplianceRequirementsRequestUserType
            The type of end user

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetComplianceRequirementsResponse
            Requirements retrieved successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.compliance.get_compliance_requirements(
                country_iso="IN",
                number_type="local",
                user_type="individual",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_compliance_requirements(
            country_iso=country_iso, number_type=number_type, user_type=user_type, request_options=request_options
        )
        return _response.data

    async def submit(
        self,
        *,
        country_iso: str,
        number_type: SubmitComplianceRequestNumberType,
        user_type: SubmitComplianceRequestUserType,
        end_user: str,
        documents: str,
        files: typing.List[core.File],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SubmitComplianceResponse:
        """
        Submit a new compliance application with end-user details and supporting documents.
        One application is allowed per organization per country per number type per user type.

        The request uses `multipart/form-data` because documents are uploaded inline.
        The `endUser` and `documents` fields are JSON strings embedded in the form data.

        Parameters
        ----------
        country_iso : str
            ISO 3166-1 alpha-2 country code

        number_type : SubmitComplianceRequestNumberType
            The type of phone number

        user_type : SubmitComplianceRequestUserType
            The type of end user

        end_user : str
            JSON-stringified end-user details. `name` is required; all other fields are optional
            but may be required by Plivo depending on country/numberType. Accepted fields:

            - `name` (required) — full name or business name
            - `lastName` — last name
            - `email` — email address
            - `addressLine1` — street address line 1
            - `addressLine2` — street address line 2
            - `city` — city
            - `state` — state or province
            - `postalCode` — postal/ZIP code
            - `country` — ISO country code; defaults to `countryIso` if omitted
            - `registrationNumber` — business registration number (required for some business applications)

        documents : str
            JSON string containing an array of document metadata. Each entry must have a
            `documentTypeId` (from the requirements endpoint) and optional `dataFields`.
            Example:
            ```json
            [{"documentTypeId": "dt_123", "dataFields": {"business_name": "Acme Corp"}}]
            ```

        files : typing.List[core.File]
            See core.File for more documentation

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SubmitComplianceResponse
            Application submitted successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.compliance.submit(
                country_iso="countryIso",
                number_type="local",
                user_type="individual",
                end_user="endUser",
                documents="documents",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit(
            country_iso=country_iso,
            number_type=number_type,
            user_type=user_type,
            end_user=end_user,
            documents=documents,
            files=files,
            request_options=request_options,
        )
        return _response.data

    async def resubmit(
        self,
        id: str,
        *,
        documents: str,
        files: typing.List[core.File],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResubmitComplianceResponse:
        """
        Resubmit a previously rejected compliance application with corrected documents.
        Only applications in `rejected` status can be resubmitted. All documents must be
        re-uploaded — partial updates are not supported.

        File/document count must match exactly. Mismatch returns 400 with message `"Expected X files, got Y"`.

        Parameters
        ----------
        id : str
            The compliance application ID

        documents : str
            JSON string containing an array of document metadata. Same format as
            the create endpoint.

        files : typing.List[core.File]
            See core.File for more documentation

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResubmitComplianceResponse
            Application resubmitted successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.compliance.resubmit(
                id="id",
                documents="documents",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.resubmit(
            id, documents=documents, files=files, request_options=request_options
        )
        return _response.data

    async def refresh_compliance_application_status(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PostComplianceApplicationsIdRefreshResponse:
        """
        Manually poll Plivo for the latest status of a compliance application.
        Use this as a fallback when webhooks are delayed. The frontend enforces a
        60-second cooldown between refreshes.

        Parameters
        ----------
        id : str
            The compliance application ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PostComplianceApplicationsIdRefreshResponse
            Application status refreshed

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.compliance.refresh_compliance_application_status(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.refresh_compliance_application_status(id, request_options=request_options)
        return _response.data
