# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from .raw_client import AsyncRawRecordingsClient, RawRecordingsClient
from .types.get_recordings_request_channel import GetRecordingsRequestChannel
from .types.get_recordings_response import GetRecordingsResponse


class RecordingsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawRecordingsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawRecordingsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawRecordingsClient
        """
        return self._raw_client

    def get(
        self,
        call_id: str,
        *,
        channel: typing.Optional[GetRecordingsRequestChannel] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetRecordingsResponse:
        """
        Returns a presigned S3 URL for the call's recording. Hand the URL straight to the customer or pull bytes server-side. The presigned URL is **time-limited** to 15 minutes; do not cache or embed it, request a fresh one each time you need the recording.

        Use `channel=mono` (default) for the composite recording (agent + caller mixed). Use `channel=dual` for a stereo recording (agent on one channel, caller on the other), which is available only when the call was captured with per-side audio. `?channel=dual` returns `404` when the dual URL is missing; retry with `?channel=mono` in that case.

        Errors:

        - `400 Invalid call ID format` if you pass a Mongo `_id` instead of the `callId` string.
        - `400 Invalid channel. Expected 'mono' or 'dual'` if `channel` is anything else.
        - `401 Unauthorized` if no API key or dashboard cookie is present.
        - `404 Call log not found` if the call is missing or belongs to another organization. (The literal `Call not found` string is only returned when the call itself is missing entirely; the util maps both to the same 404.)
        - `404 Dual-channel recording not found` if `channel=dual` was requested and only a mono capture exists.

        Parameters
        ----------
        call_id : str
            The `callId` string for the conversation (e.g. `CALL-1778226705739-7e4c17`). This is the `callId` field returned by `GET /conversation`, not the internal document id.

        channel : typing.Optional[GetRecordingsRequestChannel]
            Which channel to fetch. `mono` (default) returns the composite recording; `dual` returns the stereo recording when available.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetRecordingsResponse
            Presigned URL ready to fetch.

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.recordings.get(
            call_id="CALL-1781127346211-e765f7",
        )
        """
        _response = self._raw_client.get(call_id, channel=channel, request_options=request_options)
        return _response.data


class AsyncRecordingsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawRecordingsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawRecordingsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawRecordingsClient
        """
        return self._raw_client

    async def get(
        self,
        call_id: str,
        *,
        channel: typing.Optional[GetRecordingsRequestChannel] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetRecordingsResponse:
        """
        Returns a presigned S3 URL for the call's recording. Hand the URL straight to the customer or pull bytes server-side. The presigned URL is **time-limited** to 15 minutes; do not cache or embed it, request a fresh one each time you need the recording.

        Use `channel=mono` (default) for the composite recording (agent + caller mixed). Use `channel=dual` for a stereo recording (agent on one channel, caller on the other), which is available only when the call was captured with per-side audio. `?channel=dual` returns `404` when the dual URL is missing; retry with `?channel=mono` in that case.

        Errors:

        - `400 Invalid call ID format` if you pass a Mongo `_id` instead of the `callId` string.
        - `400 Invalid channel. Expected 'mono' or 'dual'` if `channel` is anything else.
        - `401 Unauthorized` if no API key or dashboard cookie is present.
        - `404 Call log not found` if the call is missing or belongs to another organization. (The literal `Call not found` string is only returned when the call itself is missing entirely; the util maps both to the same 404.)
        - `404 Dual-channel recording not found` if `channel=dual` was requested and only a mono capture exists.

        Parameters
        ----------
        call_id : str
            The `callId` string for the conversation (e.g. `CALL-1778226705739-7e4c17`). This is the `callId` field returned by `GET /conversation`, not the internal document id.

        channel : typing.Optional[GetRecordingsRequestChannel]
            Which channel to fetch. `mono` (default) returns the composite recording; `dual` returns the stereo recording when available.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetRecordingsResponse
            Presigned URL ready to fetch.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.recordings.get(
                call_id="CALL-1781127346211-e765f7",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(call_id, channel=channel, request_options=request_options)
        return _response.data
