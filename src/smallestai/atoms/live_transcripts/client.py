# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from .raw_client import AsyncRawLiveTranscriptsClient, RawLiveTranscriptsClient
from .types.subscribe_to_live_events_live_transcripts_response import SubscribeToLiveEventsLiveTranscriptsResponse


class LiveTranscriptsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawLiveTranscriptsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawLiveTranscriptsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawLiveTranscriptsClient
        """
        return self._raw_client

    def subscribe_to_live_events(
        self,
        *,
        call_id: str,
        organization_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Iterator[SubscribeToLiveEventsLiveTranscriptsResponse]:
        """
        Real-time streaming of user speech (STT) and agent speech (TTS) events for an active call via Server-Sent Events.

        The connection is real-time — events stream directly from the call runtime as they are produced. The SSE connection auto-closes when the call ends (`sse_close` event). Only active calls can be subscribed to; completed calls return a 400 error.

        **Transcript event types:**

        - `user_interim_transcription` — Partial, in-progress transcription as the user speaks. Use for live preview only; will be superseded by `user_transcription`.
        - `user_transcription` — Final transcription for a completed user speech turn.
        - `tts_completed` — Fired when the agent finishes speaking a TTS segment. Includes the spoken text and optionally TTS latency.

        **Lifecycle events:**

        - `sse_init` — Sent immediately when the SSE connection is established.
        - `sse_close` — Sent when the call ends, right before the server closes the connection.

        Other event types (e.g. `tool_call_start`, `pre_call_api`, `agent_log`, metrics) are also sent on this stream.

        - `call_start`
        - `call_end`
        - `turn_latency`
        - `metrics`
        - `agent_node_state`
        - `hopping`
        - `knowledgebase`
        - `variable_extraction`
        - `pre_call_api`
        - `post_call_api`
        - `agent_error`
        - `agent_log`
        - `tool_call_start`
        - `tool_call_end`
        - `tool_call_error`
        - `call_cancelled`
        - `call_recording`

        Parameters
        ----------
        call_id : str
            The call ID to subscribe events for. Missing or invalid values return 400.

        organization_id : typing.Optional[str]
            Required when using session-cookie auth. API-token auth may infer the organization from the token.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Yields
        ------
        typing.Iterator[SubscribeToLiveEventsLiveTranscriptsResponse]
            SSE event stream established successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        response = client.atoms.live_transcripts.subscribe_to_live_events(
            call_id="CALL-1758124225863-80752e",
        )
        for chunk in response:
            yield chunk
        """
        with self._raw_client.subscribe_to_live_events(
            call_id=call_id, organization_id=organization_id, request_options=request_options
        ) as r:
            yield from r.data


class AsyncLiveTranscriptsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawLiveTranscriptsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawLiveTranscriptsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawLiveTranscriptsClient
        """
        return self._raw_client

    async def subscribe_to_live_events(
        self,
        *,
        call_id: str,
        organization_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.AsyncIterator[SubscribeToLiveEventsLiveTranscriptsResponse]:
        """
        Real-time streaming of user speech (STT) and agent speech (TTS) events for an active call via Server-Sent Events.

        The connection is real-time — events stream directly from the call runtime as they are produced. The SSE connection auto-closes when the call ends (`sse_close` event). Only active calls can be subscribed to; completed calls return a 400 error.

        **Transcript event types:**

        - `user_interim_transcription` — Partial, in-progress transcription as the user speaks. Use for live preview only; will be superseded by `user_transcription`.
        - `user_transcription` — Final transcription for a completed user speech turn.
        - `tts_completed` — Fired when the agent finishes speaking a TTS segment. Includes the spoken text and optionally TTS latency.

        **Lifecycle events:**

        - `sse_init` — Sent immediately when the SSE connection is established.
        - `sse_close` — Sent when the call ends, right before the server closes the connection.

        Other event types (e.g. `tool_call_start`, `pre_call_api`, `agent_log`, metrics) are also sent on this stream.

        - `call_start`
        - `call_end`
        - `turn_latency`
        - `metrics`
        - `agent_node_state`
        - `hopping`
        - `knowledgebase`
        - `variable_extraction`
        - `pre_call_api`
        - `post_call_api`
        - `agent_error`
        - `agent_log`
        - `tool_call_start`
        - `tool_call_end`
        - `tool_call_error`
        - `call_cancelled`
        - `call_recording`

        Parameters
        ----------
        call_id : str
            The call ID to subscribe events for. Missing or invalid values return 400.

        organization_id : typing.Optional[str]
            Required when using session-cookie auth. API-token auth may infer the organization from the token.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Yields
        ------
        typing.AsyncIterator[SubscribeToLiveEventsLiveTranscriptsResponse]
            SSE event stream established successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            response = await client.atoms.live_transcripts.subscribe_to_live_events(
                call_id="CALL-1758124225863-80752e",
            )
            async for chunk in response:
                yield chunk


        asyncio.run(main())
        """
        async with self._raw_client.subscribe_to_live_events(
            call_id=call_id, organization_id=organization_id, request_options=request_options
        ) as r:
            async for _chunk in r.data:
                yield _chunk
