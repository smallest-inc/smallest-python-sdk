# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from ..types.registry_tool_definition import RegistryToolDefinition
from .raw_client import AsyncRawToolsClient, RawToolsClient
from .types.create_tool_tools_response import CreateToolToolsResponse
from .types.delete_tool_tools_response import DeleteToolToolsResponse
from .types.duplicate_tool_tools_response import DuplicateToolToolsResponse
from .types.list_tools_tools_response import ListToolsToolsResponse
from .types.update_tool_tools_response import UpdateToolToolsResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ToolsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawToolsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawToolsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawToolsClient
        """
        return self._raw_client

    def list_tools(self, *, request_options: typing.Optional[RequestOptions] = None) -> ListToolsToolsResponse:
        """
        List the organization's tools from the Tools library.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListToolsToolsResponse
            The org's tools.

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.tools.list_tools()
        """
        _response = self._raw_client.list_tools(request_options=request_options)
        return _response.data

    def create_tool(
        self, *, definition: RegistryToolDefinition, request_options: typing.Optional[RequestOptions] = None
    ) -> CreateToolToolsResponse:
        """
        Create a reusable tool in the org Tools library. Two types are supported:
        `api_call` and `client_tool` (system tools like transfer/end-call are
        configured per-agent, not here). Reference it from an agent by adding its
        returned `toolId` to `toolRefs` on the agent config.

        Referencing a tool makes it available; it does not force use. The agent only
        calls a tool when its prompt tells it to, so describe the tool's purpose and
        when to trigger it in the agent's prompt.

        Parameters
        ----------
        definition : RegistryToolDefinition
            The tool body — the same shape used inside an agent's `tools[]`
            (discriminated on `type`). `api_call` is the most common; `client_tool`
            and the built-in types are also valid.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateToolToolsResponse
            Tool created.

        Examples
        --------
        from smallestai import SmallestAI
        from smallestai.atoms import RegistryToolDefinition

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.tools.create_tool(
            definition=RegistryToolDefinition(
                type="api_call",
                name="get_order_status",
                description="Look up a customer's order by id and read back its status.",
            ),
        )
        """
        _response = self._raw_client.create_tool(definition=definition, request_options=request_options)
        return _response.data

    def delete_tool(
        self, tool_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeleteToolToolsResponse:
        """
        Delete a tool from the library. Blocked with `400` if any agent still
        references it — remove it from those agents first.

        Parameters
        ----------
        tool_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteToolToolsResponse
            Tool deleted.

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.tools.delete_tool(
            tool_id="toolId",
        )
        """
        _response = self._raw_client.delete_tool(tool_id, request_options=request_options)
        return _response.data

    def update_tool(
        self,
        tool_id: str,
        *,
        definition: RegistryToolDefinition,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateToolToolsResponse:
        """
        Update a tool's definition. Propagates live to every agent that references
        this tool (their config cache is busted on update).

        Parameters
        ----------
        tool_id : str
            The tool id, format `tool_<24 lowercase alphanumerics>`.

        definition : RegistryToolDefinition
            The tool body — the same shape used inside an agent's `tools[]`
            (discriminated on `type`). `api_call` is the most common; `client_tool`
            and the built-in types are also valid.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateToolToolsResponse
            Tool updated.

        Examples
        --------
        from smallestai import SmallestAI
        from smallestai.atoms import RegistryToolDefinition

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.tools.update_tool(
            tool_id="toolId",
            definition=RegistryToolDefinition(
                type="api_call",
                name="get_order_status",
                description="Look up a customer's order by id and read back its status.",
            ),
        )
        """
        _response = self._raw_client.update_tool(tool_id, definition=definition, request_options=request_options)
        return _response.data

    def duplicate_tool(
        self, tool_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DuplicateToolToolsResponse:
        """
        Clone a tool into a new library tool with a new `toolId`.

        Parameters
        ----------
        tool_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DuplicateToolToolsResponse
            Tool duplicated.

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.atoms.tools.duplicate_tool(
            tool_id="toolId",
        )
        """
        _response = self._raw_client.duplicate_tool(tool_id, request_options=request_options)
        return _response.data


class AsyncToolsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawToolsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawToolsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawToolsClient
        """
        return self._raw_client

    async def list_tools(self, *, request_options: typing.Optional[RequestOptions] = None) -> ListToolsToolsResponse:
        """
        List the organization's tools from the Tools library.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListToolsToolsResponse
            The org's tools.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.tools.list_tools()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_tools(request_options=request_options)
        return _response.data

    async def create_tool(
        self, *, definition: RegistryToolDefinition, request_options: typing.Optional[RequestOptions] = None
    ) -> CreateToolToolsResponse:
        """
        Create a reusable tool in the org Tools library. Two types are supported:
        `api_call` and `client_tool` (system tools like transfer/end-call are
        configured per-agent, not here). Reference it from an agent by adding its
        returned `toolId` to `toolRefs` on the agent config.

        Referencing a tool makes it available; it does not force use. The agent only
        calls a tool when its prompt tells it to, so describe the tool's purpose and
        when to trigger it in the agent's prompt.

        Parameters
        ----------
        definition : RegistryToolDefinition
            The tool body — the same shape used inside an agent's `tools[]`
            (discriminated on `type`). `api_call` is the most common; `client_tool`
            and the built-in types are also valid.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateToolToolsResponse
            Tool created.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI
        from smallestai.atoms import RegistryToolDefinition

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.tools.create_tool(
                definition=RegistryToolDefinition(
                    type="api_call",
                    name="get_order_status",
                    description="Look up a customer's order by id and read back its status.",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_tool(definition=definition, request_options=request_options)
        return _response.data

    async def delete_tool(
        self, tool_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeleteToolToolsResponse:
        """
        Delete a tool from the library. Blocked with `400` if any agent still
        references it — remove it from those agents first.

        Parameters
        ----------
        tool_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteToolToolsResponse
            Tool deleted.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.tools.delete_tool(
                tool_id="toolId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_tool(tool_id, request_options=request_options)
        return _response.data

    async def update_tool(
        self,
        tool_id: str,
        *,
        definition: RegistryToolDefinition,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateToolToolsResponse:
        """
        Update a tool's definition. Propagates live to every agent that references
        this tool (their config cache is busted on update).

        Parameters
        ----------
        tool_id : str
            The tool id, format `tool_<24 lowercase alphanumerics>`.

        definition : RegistryToolDefinition
            The tool body — the same shape used inside an agent's `tools[]`
            (discriminated on `type`). `api_call` is the most common; `client_tool`
            and the built-in types are also valid.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateToolToolsResponse
            Tool updated.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI
        from smallestai.atoms import RegistryToolDefinition

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.tools.update_tool(
                tool_id="toolId",
                definition=RegistryToolDefinition(
                    type="api_call",
                    name="get_order_status",
                    description="Look up a customer's order by id and read back its status.",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_tool(tool_id, definition=definition, request_options=request_options)
        return _response.data

    async def duplicate_tool(
        self, tool_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DuplicateToolToolsResponse:
        """
        Clone a tool into a new library tool with a new `toolId`.

        Parameters
        ----------
        tool_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DuplicateToolToolsResponse
            Tool duplicated.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.atoms.tools.duplicate_tool(
                tool_id="toolId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.duplicate_tool(tool_id, request_options=request_options)
        return _response.data
