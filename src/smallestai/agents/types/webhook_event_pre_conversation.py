# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ...core.pydantic_utilities import IS_PYDANTIC_V2
from .webhook_event_envelope import WebhookEventEnvelope
from .webhook_event_pre_conversation_metadata import WebhookEventPreConversationMetadata


class WebhookEventPreConversation(WebhookEventEnvelope):
    """
    Fired **before** the agent begins speaking. Use this to enrich CRM data,
    log call attempts, or gate outbound calls.

    Does **not** contain `callData`, `transcript`, `variables`, `analytics`,
    or `recordingUrl`. `callData` is on `post-conversation` and
    `analytics-completed`; `transcript`, `variables`, and `recordingUrl`
    are only on `post-conversation`; `analytics` is only on
    `analytics-completed`.
    """

    metadata: WebhookEventPreConversationMetadata

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
