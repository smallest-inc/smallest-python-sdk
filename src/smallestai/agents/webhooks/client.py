# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from .raw_client import AsyncRawWebhooksClient, RawWebhooksClient
from .types.create_webhooks_request_events_item import CreateWebhooksRequestEventsItem
from .types.create_webhooks_response import CreateWebhooksResponse
from .types.delete_agent_agent_id_webhook_subscriptions_response import DeleteAgentAgentIdWebhookSubscriptionsResponse
from .types.delete_webhooks_response import DeleteWebhooksResponse
from .types.get_agent_agent_id_webhook_subscriptions_response import GetAgentAgentIdWebhookSubscriptionsResponse
from .types.get_webhook_response import GetWebhookResponse
from .types.post_agent_agent_id_webhook_subscriptions_request_event_types_item import (
    PostAgentAgentIdWebhookSubscriptionsRequestEventTypesItem,
)
from .types.post_agent_agent_id_webhook_subscriptions_response import PostAgentAgentIdWebhookSubscriptionsResponse
from .types.update_webhooks_response import UpdateWebhooksResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class WebhooksClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawWebhooksClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawWebhooksClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawWebhooksClient
        """
        return self._raw_client

    def get_webhooks(
        self, *, webhook_id: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> GetWebhookResponse:
        """
        Retrieve all webhooks for the organization or a specific webhook by ID

        Parameters
        ----------
        webhook_id : typing.Optional[str]
            Optional MongoDB ObjectId (24-char hex) of a specific webhook to retrieve. If omitted, returns all webhooks for the organization.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetWebhookResponse
            Successful response

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.agents.webhooks.get_webhooks()
        """
        _response = self._raw_client.get_webhooks(webhook_id=webhook_id, request_options=request_options)
        return _response.data

    def create(
        self,
        *,
        endpoint: str,
        description: str,
        events: typing.Sequence[CreateWebhooksRequestEventsItem],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateWebhooksResponse:
        """
        Create a new webhook with subscriptions for specific agents and events.

        **400 is also returned when the endpoint URL is already registered:**
        `"A webhook with this URL has already been registered"`

        Parameters
        ----------
        endpoint : str
            The webhook endpoint URL

        description : str
            The description of the webhook

        events : typing.Sequence[CreateWebhooksRequestEventsItem]
            Array of events to subscribe to

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateWebhooksResponse
            Webhook created successfully

        Examples
        --------
        from smallestai import SmallestAI
        from smallestai.agents.webhooks import CreateWebhooksRequestEventsItem

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.agents.webhooks.create(
            endpoint="https://example.com/webhook",
            description="Webhook for conversation events",
            events=[
                CreateWebhooksRequestEventsItem(
                    agent_id="60d0fe4f5311236168a109ca",
                    event_type="pre-conversation",
                )
            ],
        )
        """
        _response = self._raw_client.create(
            endpoint=endpoint, description=description, events=events, request_options=request_options
        )
        return _response.data

    def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> DeleteWebhooksResponse:
        """
        Delete a webhook by its ID.

        **400 is returned when the webhook still has active agent subscriptions:**
        `"Cannot delete webhook: It is currently assigned to one or more agents. Please remove all agent assignments first."`
        Call `DELETE /agent/{agentId}/webhook-subscriptions` for each assigned agent before deleting.

        **400 is also returned for an invalid webhook ID format:**
        `"The provided Webhook ID is invalid."`

        Parameters
        ----------
        id : str
            The ID of the webhook to delete

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteWebhooksResponse
            Webhook deleted successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.agents.webhooks.delete(
            id="id",
        )
        """
        _response = self._raw_client.delete(id, request_options=request_options)
        return _response.data

    def update(
        self,
        id: str,
        *,
        endpoint: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        headers: typing.Optional[typing.Dict[str, str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateWebhooksResponse:
        """
        Update a webhook's endpoint URL, description, or custom headers. At
        least one of the three fields must be present in the request body.

        **Event subscriptions cannot be changed here.** To add or remove an
        agent's subscription to this webhook, use `POST /agent/{agentId}/webhook-subscriptions`
        and `DELETE /agent/{agentId}/webhook-subscriptions`.

        **Custom `headers` behavior**
        - Send a non-empty object to replace all custom headers on the webhook.
        - Send an empty object (`{}`) to clear all custom headers.
        - Omit the field to leave existing custom headers untouched.

        Custom header limits: at most 10 headers per webhook, values up to
        1024 characters, header names must match RFC 7230 token syntax. The
        following names are reserved and rejected: `x-signature`, `host`,
        `content-length`, `content-type`, `connection`, `transfer-encoding`.

        Parameters
        ----------
        id : str
            The ID of the webhook to update.

        endpoint : typing.Optional[str]
            New endpoint URL. Must be a valid URL.

        description : typing.Optional[str]
            New human-readable label.

        headers : typing.Optional[typing.Dict[str, str]]
            Map of custom header names to values. Non-empty object replaces
            all existing headers; empty object clears them.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateWebhooksResponse
            Webhook updated successfully.

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.agents.webhooks.update(
            id="id",
        )
        """
        _response = self._raw_client.update(
            id, endpoint=endpoint, description=description, headers=headers, request_options=request_options
        )
        return _response.data

    def get_webhook_subscriptions_for_an_agent(
        self, agent_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GetAgentAgentIdWebhookSubscriptionsResponse:
        """
        Retrieve webhook subscriptions for a given agent ID

        Parameters
        ----------
        agent_id : str
            The ID of the agent

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentAgentIdWebhookSubscriptionsResponse
            Successful response

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.agents.webhooks.get_webhook_subscriptions_for_an_agent(
            agent_id="agentId",
        )
        """
        _response = self._raw_client.get_webhook_subscriptions_for_an_agent(agent_id, request_options=request_options)
        return _response.data

    def replace_webhook_subscriptions_for_an_agent(
        self,
        agent_id: str,
        *,
        event_types: typing.Sequence[PostAgentAgentIdWebhookSubscriptionsRequestEventTypesItem],
        webhook_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PostAgentAgentIdWebhookSubscriptionsResponse:
        """
        **Replaces** all existing webhook subscriptions for the agent with the provided event types.
        Any previously configured subscriptions for this agent are deleted before the new ones are created.
        To add subscriptions without removing existing ones, retrieve current subscriptions first and include them in the request.

        Parameters
        ----------
        agent_id : str
            The ID of the agent to create subscriptions for

        event_types : typing.Sequence[PostAgentAgentIdWebhookSubscriptionsRequestEventTypesItem]
            Array of event types to subscribe to

        webhook_id : str
            The ID of the webhook to subscribe to

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PostAgentAgentIdWebhookSubscriptionsResponse
            Subscriptions created successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.agents.webhooks.replace_webhook_subscriptions_for_an_agent(
            agent_id="agentId",
            event_types=["pre-conversation"],
            webhook_id="60d0fe4f5311236168a109ca",
        )
        """
        _response = self._raw_client.replace_webhook_subscriptions_for_an_agent(
            agent_id, event_types=event_types, webhook_id=webhook_id, request_options=request_options
        )
        return _response.data

    def delete_webhook_subscriptions_for_an_agent(
        self, agent_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeleteAgentAgentIdWebhookSubscriptionsResponse:
        """
        Deletes **all** webhook subscriptions for the agent, regardless of which webhook they belong to.
        If the agent has subscriptions across multiple webhooks, all of them are removed in a single call.

        Parameters
        ----------
        agent_id : str
            The ID of the agent to filter subscriptions by

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteAgentAgentIdWebhookSubscriptionsResponse
            Subscriptions deleted successfully

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.agents.webhooks.delete_webhook_subscriptions_for_an_agent(
            agent_id="agentId",
        )
        """
        _response = self._raw_client.delete_webhook_subscriptions_for_an_agent(
            agent_id, request_options=request_options
        )
        return _response.data


class AsyncWebhooksClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawWebhooksClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawWebhooksClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawWebhooksClient
        """
        return self._raw_client

    async def get_webhooks(
        self, *, webhook_id: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> GetWebhookResponse:
        """
        Retrieve all webhooks for the organization or a specific webhook by ID

        Parameters
        ----------
        webhook_id : typing.Optional[str]
            Optional MongoDB ObjectId (24-char hex) of a specific webhook to retrieve. If omitted, returns all webhooks for the organization.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetWebhookResponse
            Successful response

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.webhooks.get_webhooks()


        asyncio.run(main())
        """
        _response = await self._raw_client.get_webhooks(webhook_id=webhook_id, request_options=request_options)
        return _response.data

    async def create(
        self,
        *,
        endpoint: str,
        description: str,
        events: typing.Sequence[CreateWebhooksRequestEventsItem],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateWebhooksResponse:
        """
        Create a new webhook with subscriptions for specific agents and events.

        **400 is also returned when the endpoint URL is already registered:**
        `"A webhook with this URL has already been registered"`

        Parameters
        ----------
        endpoint : str
            The webhook endpoint URL

        description : str
            The description of the webhook

        events : typing.Sequence[CreateWebhooksRequestEventsItem]
            Array of events to subscribe to

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateWebhooksResponse
            Webhook created successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI
        from smallestai.agents.webhooks import CreateWebhooksRequestEventsItem

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.webhooks.create(
                endpoint="https://example.com/webhook",
                description="Webhook for conversation events",
                events=[
                    CreateWebhooksRequestEventsItem(
                        agent_id="60d0fe4f5311236168a109ca",
                        event_type="pre-conversation",
                    )
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            endpoint=endpoint, description=description, events=events, request_options=request_options
        )
        return _response.data

    async def delete(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeleteWebhooksResponse:
        """
        Delete a webhook by its ID.

        **400 is returned when the webhook still has active agent subscriptions:**
        `"Cannot delete webhook: It is currently assigned to one or more agents. Please remove all agent assignments first."`
        Call `DELETE /agent/{agentId}/webhook-subscriptions` for each assigned agent before deleting.

        **400 is also returned for an invalid webhook ID format:**
        `"The provided Webhook ID is invalid."`

        Parameters
        ----------
        id : str
            The ID of the webhook to delete

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteWebhooksResponse
            Webhook deleted successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.webhooks.delete(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(id, request_options=request_options)
        return _response.data

    async def update(
        self,
        id: str,
        *,
        endpoint: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        headers: typing.Optional[typing.Dict[str, str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateWebhooksResponse:
        """
        Update a webhook's endpoint URL, description, or custom headers. At
        least one of the three fields must be present in the request body.

        **Event subscriptions cannot be changed here.** To add or remove an
        agent's subscription to this webhook, use `POST /agent/{agentId}/webhook-subscriptions`
        and `DELETE /agent/{agentId}/webhook-subscriptions`.

        **Custom `headers` behavior**
        - Send a non-empty object to replace all custom headers on the webhook.
        - Send an empty object (`{}`) to clear all custom headers.
        - Omit the field to leave existing custom headers untouched.

        Custom header limits: at most 10 headers per webhook, values up to
        1024 characters, header names must match RFC 7230 token syntax. The
        following names are reserved and rejected: `x-signature`, `host`,
        `content-length`, `content-type`, `connection`, `transfer-encoding`.

        Parameters
        ----------
        id : str
            The ID of the webhook to update.

        endpoint : typing.Optional[str]
            New endpoint URL. Must be a valid URL.

        description : typing.Optional[str]
            New human-readable label.

        headers : typing.Optional[typing.Dict[str, str]]
            Map of custom header names to values. Non-empty object replaces
            all existing headers; empty object clears them.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateWebhooksResponse
            Webhook updated successfully.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.webhooks.update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            id, endpoint=endpoint, description=description, headers=headers, request_options=request_options
        )
        return _response.data

    async def get_webhook_subscriptions_for_an_agent(
        self, agent_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GetAgentAgentIdWebhookSubscriptionsResponse:
        """
        Retrieve webhook subscriptions for a given agent ID

        Parameters
        ----------
        agent_id : str
            The ID of the agent

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetAgentAgentIdWebhookSubscriptionsResponse
            Successful response

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.webhooks.get_webhook_subscriptions_for_an_agent(
                agent_id="agentId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_webhook_subscriptions_for_an_agent(
            agent_id, request_options=request_options
        )
        return _response.data

    async def replace_webhook_subscriptions_for_an_agent(
        self,
        agent_id: str,
        *,
        event_types: typing.Sequence[PostAgentAgentIdWebhookSubscriptionsRequestEventTypesItem],
        webhook_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PostAgentAgentIdWebhookSubscriptionsResponse:
        """
        **Replaces** all existing webhook subscriptions for the agent with the provided event types.
        Any previously configured subscriptions for this agent are deleted before the new ones are created.
        To add subscriptions without removing existing ones, retrieve current subscriptions first and include them in the request.

        Parameters
        ----------
        agent_id : str
            The ID of the agent to create subscriptions for

        event_types : typing.Sequence[PostAgentAgentIdWebhookSubscriptionsRequestEventTypesItem]
            Array of event types to subscribe to

        webhook_id : str
            The ID of the webhook to subscribe to

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PostAgentAgentIdWebhookSubscriptionsResponse
            Subscriptions created successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.webhooks.replace_webhook_subscriptions_for_an_agent(
                agent_id="agentId",
                event_types=["pre-conversation"],
                webhook_id="60d0fe4f5311236168a109ca",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.replace_webhook_subscriptions_for_an_agent(
            agent_id, event_types=event_types, webhook_id=webhook_id, request_options=request_options
        )
        return _response.data

    async def delete_webhook_subscriptions_for_an_agent(
        self, agent_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeleteAgentAgentIdWebhookSubscriptionsResponse:
        """
        Deletes **all** webhook subscriptions for the agent, regardless of which webhook they belong to.
        If the agent has subscriptions across multiple webhooks, all of them are removed in a single call.

        Parameters
        ----------
        agent_id : str
            The ID of the agent to filter subscriptions by

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteAgentAgentIdWebhookSubscriptionsResponse
            Subscriptions deleted successfully

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agents.webhooks.delete_webhook_subscriptions_for_an_agent(
                agent_id="agentId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_webhook_subscriptions_for_an_agent(
            agent_id, request_options=request_options
        )
        return _response.data
