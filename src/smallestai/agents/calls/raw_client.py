# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing
from json.decoder import JSONDecodeError

from ...core.api_error import ApiError
from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.datetime_utils import serialize_datetime
from ...core.http_response import AsyncHttpResponse, HttpResponse
from ...core.jsonable_encoder import encode_path_param
from ...core.parse_error import ParsingError
from ...core.request_options import RequestOptions
from ...core.serialization import convert_and_respect_annotation_metadata
from ...core.unchecked_base_model import construct_type
from ..errors.bad_request_error import BadRequestError
from ..errors.forbidden_error import ForbiddenError
from ..errors.internal_server_error import InternalServerError
from ..errors.not_found_error import NotFoundError
from ..errors.unauthorized_error import UnauthorizedError
from .types.get_calls_response import GetCallsResponse
from .types.list_calls_request_call_types import ListCallsRequestCallTypes
from .types.list_calls_request_sort_by import ListCallsRequestSortBy
from .types.list_calls_request_sort_order import ListCallsRequestSortOrder
from .types.list_calls_response import ListCallsResponse
from .types.search_calls_response import SearchCallsResponse
from .types.start_outbound_call_calls_request_variables_value import StartOutboundCallCallsRequestVariablesValue
from .types.start_outbound_call_calls_request_x_test_call import StartOutboundCallCallsRequestXTestCall
from .types.start_outbound_call_calls_response import StartOutboundCallCallsResponse
from pydantic import ValidationError

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class RawCallsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._client_wrapper = client_wrapper

    def list(
        self,
        *,
        page: typing.Optional[int] = None,
        limit: typing.Optional[int] = None,
        agent_ids: typing.Optional[str] = None,
        campaign_ids: typing.Optional[str] = None,
        call_types: typing.Optional[ListCallsRequestCallTypes] = None,
        search: typing.Optional[str] = None,
        status_filter: typing.Optional[str] = None,
        disconnect_reason_filter: typing.Optional[str] = None,
        call_attempt_filter: typing.Optional[str] = None,
        duration_filter: typing.Optional[str] = None,
        sort_by: typing.Optional[ListCallsRequestSortBy] = None,
        sort_order: typing.Optional[ListCallsRequestSortOrder] = None,
        date_from: typing.Optional[dt.datetime] = None,
        date_to: typing.Optional[dt.datetime] = None,
        version_filter: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[ListCallsResponse]:
        """
        Retrieve paginated conversation logs with support for various filters. Returns call logs for agents belonging to the authenticated user's organization.

        Parameters
        ----------
        page : typing.Optional[int]
            Page number for pagination

        limit : typing.Optional[int]
            Number of items per page. Server-side cap is 500 — values above 500 are silently clamped.

        agent_ids : typing.Optional[str]
            Comma-separated list of agent IDs to filter by

        campaign_ids : typing.Optional[str]
            Comma-separated list of campaign IDs to filter by

        call_types : typing.Optional[ListCallsRequestCallTypes]
            Comma-separated list of call types to filter by

        search : typing.Optional[str]
            Search query to filter by callId, fromNumber, or toNumber

        status_filter : typing.Optional[str]
            Comma-separated list of call statuses to filter by.
            Available statuses: pending, in_progress, in_queue, processing, active, completed, failed, no_answer, cancelled

        disconnect_reason_filter : typing.Optional[str]
            Comma-separated list of disconnect reasons to filter by.
            Available reasons: user_hangup, agent_hangup, connection_error, timeout, system_error, transfer_complete

        call_attempt_filter : typing.Optional[str]
            Comma-separated list of call attempt types to filter by.
            Available filters: initial (first attempt calls), retry (retry attempt calls), all (all calls)

        duration_filter : typing.Optional[str]
            Comma-separated list of duration ranges to filter by.
            Available ranges: 0-30 (0-30 seconds), 30-60 (30-60 seconds), 1-5 (1-5 minutes), 5+ (more than 5 minutes)

        sort_by : typing.Optional[ListCallsRequestSortBy]
            Field to sort results by

        sort_order : typing.Optional[ListCallsRequestSortOrder]
            Sort direction

        date_from : typing.Optional[dt.datetime]
            ISO date — return calls created on or after this date

        date_to : typing.Optional[dt.datetime]
            ISO date — return calls created on or before this date

        version_filter : typing.Optional[str]
            Comma-separated version IDs to filter calls by the agent version that handled them

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[ListCallsResponse]
            Successful response
        """
        _response = self._client_wrapper.httpx_client.request(
            "conversation",
            base_url=self._client_wrapper.get_environment().atoms,
            method="GET",
            params={
                "page": page,
                "limit": limit,
                "agentIds": agent_ids,
                "campaignIds": campaign_ids,
                "callTypes": call_types,
                "search": search,
                "statusFilter": status_filter,
                "disconnectReasonFilter": disconnect_reason_filter,
                "callAttemptFilter": call_attempt_filter,
                "durationFilter": duration_filter,
                "sortBy": sort_by,
                "sortOrder": sort_order,
                "dateFrom": serialize_datetime(date_from) if date_from is not None else None,
                "dateTo": serialize_datetime(date_to) if date_to is not None else None,
                "versionFilter": version_filter,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ListCallsResponse,
                    construct_type(
                        type_=ListCallsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 500:
                raise InternalServerError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def search(
        self, *, call_ids: typing.Sequence[str], request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[SearchCallsResponse]:
        """
        Fetch specific conversation logs by their callIds. This endpoint allows you to retrieve up to 100 specific calls at once.
        Only returns calls that belong to agents in your organization (security check enforced).
        Unlike the GET /conversation endpoint, this endpoint can also return retry calls (non-root calls).

        **Differences from GET /conversation response:** each log item has the same base structure but
        the following three fields are **not** included here:
        - `dispositionMetrics` — not enriched
        - `agentDispositionConfig` — not enriched
        - `versionNumber` — not enriched

        Parameters
        ----------
        call_ids : typing.Sequence[str]
            Array of callIds to fetch. Format: `CALL-{13-digit-timestamp}-{6-char-hex}`
            (e.g. `CALL-1737000000000-abc123`). Minimum 1, maximum 100 per request.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[SearchCallsResponse]
            Successful response
        """
        _response = self._client_wrapper.httpx_client.request(
            "conversation/search",
            base_url=self._client_wrapper.get_environment().atoms,
            method="POST",
            json={
                "callIds": call_ids,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SearchCallsResponse,
                    construct_type(
                        type_=SearchCallsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 500:
                raise InternalServerError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def get(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[GetCallsResponse]:
        """
        Retrieve detailed information about a specific conversation including transcript, events, and latency metrics.

        Parameters
        ----------
        id : str
            The callId of the conversation (format `CALL-{13-digit-timestamp}-{6-char-hex}`). You can get the callId from the conversation logs endpoint.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[GetCallsResponse]
            Successful response
        """
        _response = self._client_wrapper.httpx_client.request(
            f"conversation/{encode_path_param(id)}",
            base_url=self._client_wrapper.get_environment().atoms,
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    GetCallsResponse,
                    construct_type(
                        type_=GetCallsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 500:
                raise InternalServerError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def start_outbound_call(
        self,
        *,
        agent_id: str,
        phone_number: str,
        test_call: typing.Optional[StartOutboundCallCallsRequestXTestCall] = None,
        variables: typing.Optional[typing.Dict[str, StartOutboundCallCallsRequestVariablesValue]] = OMIT,
        from_product_id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        operator_id: typing.Optional[str] = OMIT,
        operator_data: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[StartOutboundCallCallsResponse]:
        """
        Initiates an outbound telephony call with a specified agent and phone number.

        ## Caller-ID resolution

        When `fromProductId` is omitted **and** the agent has no telephony
        product attached, the call dispatches from a Smallest-managed Plivo
        trunk using a default caller-ID number (chosen by destination
        country). The call still places and the response is still
        `200 + conversationId`, but the recipient sees the default Smallest
        number rather than your own. For production traffic, either:

        - pass `fromProductId` explicitly (look up your owned numbers via
          `GET /product/phone-numbers`), or
        - attach a phone-number product to the agent.

        ## Resolved-config check

        The call uses the agent's currently-active version. If your most
        recent prompt change went through `PATCH /workflow/{workflowId}` and
        the agent has versioning enabled, that change may not have
        propagated to the active version — and the call will play the
        platform-default greeting instead of your prompt. Before placing a
        production call, fetch `GET /agent/{agentId}` and confirm
        `_resolvedConfig.firstMessage` (and related fields) match what you
        intended. The
        [Versioning Lifecycle](/atoms/developer-guide/build/agents/versioning-lifecycle)
        guide covers the correct edit flow.

        **400 is returned for:**
        - Invalid `agentId` format (`"Invalid agent id"`)
        - Invalid `phoneNumber` format (`"Invalid phone number"`)
        - Invalid `fromProductId` format (`"Invalid product id"`)
        - Agent not found or not in the caller's org (`"Agent not found"`)
        - Agent is archived (`"Agent is archived and cannot initiate calls"`)
        - `workflow_graph` agent has no workflow configured (`"Workflow not found"`)
        - Workflow has validation errors (`"Invalid workflow, please fix the errors..."`)

        **403** is returned for `workflow_graph` agents when the org lacks conversational agents access.

        **Test calls:** set the `x-test-call: true` header to mark the resulting call log as a test call
        (`isTest: true`). Test calls are subject to concurrent slot limits.

        Parameters
        ----------
        agent_id : str
            MongoDB ObjectId of the agent initiating the conversation

        phone_number : str
            The E.164 phone number to call

        test_call : typing.Optional[StartOutboundCallCallsRequestXTestCall]
            Set to "true" to mark this as a test call. The call log will have isTest=true and counts against concurrent test-call slot limits.

        variables : typing.Optional[typing.Dict[str, StartOutboundCallCallsRequestVariablesValue]]
            Variables to inject into the agent's prompt at call time.
            Values must be string, number, or boolean — nested objects are not supported.

        from_product_id : typing.Optional[str]
            ID of the telephony product (phone number) to call from. Get this from `GET /product/phone-numbers`.

        version_id : typing.Optional[str]
            ID of a specific published agent version to use for this call.
            Useful for test calls — attributes the call log to that version so you can track
            which version was tested.

        operator_id : typing.Optional[str]
            Integration operator identifier. Pass `"webengage"` to trigger the WebEngage
            integration flow.

        operator_data : typing.Optional[typing.Dict[str, typing.Any]]
            Arbitrary data passed to the operator (e.g. `userId`, `journeyId` for WebEngage).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[StartOutboundCallCallsResponse]
            Successfully started the outbound conversation
        """
        _response = self._client_wrapper.httpx_client.request(
            "conversation/outbound",
            base_url=self._client_wrapper.get_environment().atoms,
            method="POST",
            json={
                "agentId": agent_id,
                "phoneNumber": phone_number,
                "variables": convert_and_respect_annotation_metadata(
                    object_=variables,
                    annotation=typing.Dict[str, StartOutboundCallCallsRequestVariablesValue],
                    direction="write",
                ),
                "fromProductId": from_product_id,
                "versionId": version_id,
                "operatorId": operator_id,
                "operatorData": operator_data,
            },
            headers={
                "content-type": "application/json",
                "x-test-call": str(test_call) if test_call is not None else None,
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    StartOutboundCallCallsResponse,
                    construct_type(
                        type_=StartOutboundCallCallsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 500:
                raise InternalServerError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)


class AsyncRawCallsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._client_wrapper = client_wrapper

    async def list(
        self,
        *,
        page: typing.Optional[int] = None,
        limit: typing.Optional[int] = None,
        agent_ids: typing.Optional[str] = None,
        campaign_ids: typing.Optional[str] = None,
        call_types: typing.Optional[ListCallsRequestCallTypes] = None,
        search: typing.Optional[str] = None,
        status_filter: typing.Optional[str] = None,
        disconnect_reason_filter: typing.Optional[str] = None,
        call_attempt_filter: typing.Optional[str] = None,
        duration_filter: typing.Optional[str] = None,
        sort_by: typing.Optional[ListCallsRequestSortBy] = None,
        sort_order: typing.Optional[ListCallsRequestSortOrder] = None,
        date_from: typing.Optional[dt.datetime] = None,
        date_to: typing.Optional[dt.datetime] = None,
        version_filter: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[ListCallsResponse]:
        """
        Retrieve paginated conversation logs with support for various filters. Returns call logs for agents belonging to the authenticated user's organization.

        Parameters
        ----------
        page : typing.Optional[int]
            Page number for pagination

        limit : typing.Optional[int]
            Number of items per page. Server-side cap is 500 — values above 500 are silently clamped.

        agent_ids : typing.Optional[str]
            Comma-separated list of agent IDs to filter by

        campaign_ids : typing.Optional[str]
            Comma-separated list of campaign IDs to filter by

        call_types : typing.Optional[ListCallsRequestCallTypes]
            Comma-separated list of call types to filter by

        search : typing.Optional[str]
            Search query to filter by callId, fromNumber, or toNumber

        status_filter : typing.Optional[str]
            Comma-separated list of call statuses to filter by.
            Available statuses: pending, in_progress, in_queue, processing, active, completed, failed, no_answer, cancelled

        disconnect_reason_filter : typing.Optional[str]
            Comma-separated list of disconnect reasons to filter by.
            Available reasons: user_hangup, agent_hangup, connection_error, timeout, system_error, transfer_complete

        call_attempt_filter : typing.Optional[str]
            Comma-separated list of call attempt types to filter by.
            Available filters: initial (first attempt calls), retry (retry attempt calls), all (all calls)

        duration_filter : typing.Optional[str]
            Comma-separated list of duration ranges to filter by.
            Available ranges: 0-30 (0-30 seconds), 30-60 (30-60 seconds), 1-5 (1-5 minutes), 5+ (more than 5 minutes)

        sort_by : typing.Optional[ListCallsRequestSortBy]
            Field to sort results by

        sort_order : typing.Optional[ListCallsRequestSortOrder]
            Sort direction

        date_from : typing.Optional[dt.datetime]
            ISO date — return calls created on or after this date

        date_to : typing.Optional[dt.datetime]
            ISO date — return calls created on or before this date

        version_filter : typing.Optional[str]
            Comma-separated version IDs to filter calls by the agent version that handled them

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[ListCallsResponse]
            Successful response
        """
        _response = await self._client_wrapper.httpx_client.request(
            "conversation",
            base_url=self._client_wrapper.get_environment().atoms,
            method="GET",
            params={
                "page": page,
                "limit": limit,
                "agentIds": agent_ids,
                "campaignIds": campaign_ids,
                "callTypes": call_types,
                "search": search,
                "statusFilter": status_filter,
                "disconnectReasonFilter": disconnect_reason_filter,
                "callAttemptFilter": call_attempt_filter,
                "durationFilter": duration_filter,
                "sortBy": sort_by,
                "sortOrder": sort_order,
                "dateFrom": serialize_datetime(date_from) if date_from is not None else None,
                "dateTo": serialize_datetime(date_to) if date_to is not None else None,
                "versionFilter": version_filter,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ListCallsResponse,
                    construct_type(
                        type_=ListCallsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 500:
                raise InternalServerError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def search(
        self, *, call_ids: typing.Sequence[str], request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[SearchCallsResponse]:
        """
        Fetch specific conversation logs by their callIds. This endpoint allows you to retrieve up to 100 specific calls at once.
        Only returns calls that belong to agents in your organization (security check enforced).
        Unlike the GET /conversation endpoint, this endpoint can also return retry calls (non-root calls).

        **Differences from GET /conversation response:** each log item has the same base structure but
        the following three fields are **not** included here:
        - `dispositionMetrics` — not enriched
        - `agentDispositionConfig` — not enriched
        - `versionNumber` — not enriched

        Parameters
        ----------
        call_ids : typing.Sequence[str]
            Array of callIds to fetch. Format: `CALL-{13-digit-timestamp}-{6-char-hex}`
            (e.g. `CALL-1737000000000-abc123`). Minimum 1, maximum 100 per request.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[SearchCallsResponse]
            Successful response
        """
        _response = await self._client_wrapper.httpx_client.request(
            "conversation/search",
            base_url=self._client_wrapper.get_environment().atoms,
            method="POST",
            json={
                "callIds": call_ids,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SearchCallsResponse,
                    construct_type(
                        type_=SearchCallsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 500:
                raise InternalServerError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def get(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[GetCallsResponse]:
        """
        Retrieve detailed information about a specific conversation including transcript, events, and latency metrics.

        Parameters
        ----------
        id : str
            The callId of the conversation (format `CALL-{13-digit-timestamp}-{6-char-hex}`). You can get the callId from the conversation logs endpoint.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[GetCallsResponse]
            Successful response
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"conversation/{encode_path_param(id)}",
            base_url=self._client_wrapper.get_environment().atoms,
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    GetCallsResponse,
                    construct_type(
                        type_=GetCallsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 500:
                raise InternalServerError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def start_outbound_call(
        self,
        *,
        agent_id: str,
        phone_number: str,
        test_call: typing.Optional[StartOutboundCallCallsRequestXTestCall] = None,
        variables: typing.Optional[typing.Dict[str, StartOutboundCallCallsRequestVariablesValue]] = OMIT,
        from_product_id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        operator_id: typing.Optional[str] = OMIT,
        operator_data: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[StartOutboundCallCallsResponse]:
        """
        Initiates an outbound telephony call with a specified agent and phone number.

        ## Caller-ID resolution

        When `fromProductId` is omitted **and** the agent has no telephony
        product attached, the call dispatches from a Smallest-managed Plivo
        trunk using a default caller-ID number (chosen by destination
        country). The call still places and the response is still
        `200 + conversationId`, but the recipient sees the default Smallest
        number rather than your own. For production traffic, either:

        - pass `fromProductId` explicitly (look up your owned numbers via
          `GET /product/phone-numbers`), or
        - attach a phone-number product to the agent.

        ## Resolved-config check

        The call uses the agent's currently-active version. If your most
        recent prompt change went through `PATCH /workflow/{workflowId}` and
        the agent has versioning enabled, that change may not have
        propagated to the active version — and the call will play the
        platform-default greeting instead of your prompt. Before placing a
        production call, fetch `GET /agent/{agentId}` and confirm
        `_resolvedConfig.firstMessage` (and related fields) match what you
        intended. The
        [Versioning Lifecycle](/atoms/developer-guide/build/agents/versioning-lifecycle)
        guide covers the correct edit flow.

        **400 is returned for:**
        - Invalid `agentId` format (`"Invalid agent id"`)
        - Invalid `phoneNumber` format (`"Invalid phone number"`)
        - Invalid `fromProductId` format (`"Invalid product id"`)
        - Agent not found or not in the caller's org (`"Agent not found"`)
        - Agent is archived (`"Agent is archived and cannot initiate calls"`)
        - `workflow_graph` agent has no workflow configured (`"Workflow not found"`)
        - Workflow has validation errors (`"Invalid workflow, please fix the errors..."`)

        **403** is returned for `workflow_graph` agents when the org lacks conversational agents access.

        **Test calls:** set the `x-test-call: true` header to mark the resulting call log as a test call
        (`isTest: true`). Test calls are subject to concurrent slot limits.

        Parameters
        ----------
        agent_id : str
            MongoDB ObjectId of the agent initiating the conversation

        phone_number : str
            The E.164 phone number to call

        test_call : typing.Optional[StartOutboundCallCallsRequestXTestCall]
            Set to "true" to mark this as a test call. The call log will have isTest=true and counts against concurrent test-call slot limits.

        variables : typing.Optional[typing.Dict[str, StartOutboundCallCallsRequestVariablesValue]]
            Variables to inject into the agent's prompt at call time.
            Values must be string, number, or boolean — nested objects are not supported.

        from_product_id : typing.Optional[str]
            ID of the telephony product (phone number) to call from. Get this from `GET /product/phone-numbers`.

        version_id : typing.Optional[str]
            ID of a specific published agent version to use for this call.
            Useful for test calls — attributes the call log to that version so you can track
            which version was tested.

        operator_id : typing.Optional[str]
            Integration operator identifier. Pass `"webengage"` to trigger the WebEngage
            integration flow.

        operator_data : typing.Optional[typing.Dict[str, typing.Any]]
            Arbitrary data passed to the operator (e.g. `userId`, `journeyId` for WebEngage).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[StartOutboundCallCallsResponse]
            Successfully started the outbound conversation
        """
        _response = await self._client_wrapper.httpx_client.request(
            "conversation/outbound",
            base_url=self._client_wrapper.get_environment().atoms,
            method="POST",
            json={
                "agentId": agent_id,
                "phoneNumber": phone_number,
                "variables": convert_and_respect_annotation_metadata(
                    object_=variables,
                    annotation=typing.Dict[str, StartOutboundCallCallsRequestVariablesValue],
                    direction="write",
                ),
                "fromProductId": from_product_id,
                "versionId": version_id,
                "operatorId": operator_id,
                "operatorData": operator_data,
            },
            headers={
                "content-type": "application/json",
                "x-test-call": str(test_call) if test_call is not None else None,
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    StartOutboundCallCallsResponse,
                    construct_type(
                        type_=StartOutboundCallCallsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 500:
                raise InternalServerError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        construct_type(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)
