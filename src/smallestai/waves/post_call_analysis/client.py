# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from ..types.disposition_metric import DispositionMetric
from ..types.pca_response import PcaResponse
from .raw_client import AsyncRawPostCallAnalysisClient, RawPostCallAnalysisClient
from .types.generate_post_call_analysis_response import GeneratePostCallAnalysisResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class PostCallAnalysisClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPostCallAnalysisClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPostCallAnalysisClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPostCallAnalysisClient
        """
        return self._raw_client

    def analyze(
        self,
        *,
        transcript: str,
        disposition_metrics: typing.Optional[typing.Sequence[DispositionMetric]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PcaResponse:
        """
        Analyze a call transcript and extract structured disposition metrics. Provide the
        transcript and the metrics you want extracted; the response returns a short summary
        plus one result per requested metric, each with a value, a confidence score, and the
        transcript evidence for it. Values are grounded strictly in the transcript (no
        inference); when evidence is absent, the type's fallback is used.

        Parameters
        ----------
        transcript : str
            The full call transcript to analyze.

        disposition_metrics : typing.Optional[typing.Sequence[DispositionMetric]]
            The metrics to extract. Omit or pass an empty array to get just a summary.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PcaResponse
            Analysis complete.

        Examples
        --------
        from smallestai import SmallestAI
        from smallestai.waves import DispositionMetric

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.waves.post_call_analysis.analyze(
            transcript="User: I want a refund for my torn jacket. Agent: I have logged your complaint and issued a full refund of $80.",
            disposition_metrics=[
                DispositionMetric(
                    identifier="refund_issued",
                    disposition_metric_prompt="Was a refund issued to the customer?",
                    disposition_metric_type="BOOLEAN",
                )
            ],
        )
        """
        _response = self._raw_client.analyze(
            transcript=transcript, disposition_metrics=disposition_metrics, request_options=request_options
        )
        return _response.data

    def generate(
        self, *, prompt: str, request_options: typing.Optional[RequestOptions] = None
    ) -> GeneratePostCallAnalysisResponse:
        """
        Run a single-prompt generation and get the model's text output. A lightweight
        text-in / text-out endpoint (no chat history); for multi-turn chat use the
        Electron chat completions API instead.

        Parameters
        ----------
        prompt : str
            The prompt to generate from.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeneratePostCallAnalysisResponse
            Generation complete.

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.waves.post_call_analysis.generate(
            prompt="Say hello in exactly three words.",
        )
        """
        _response = self._raw_client.generate(prompt=prompt, request_options=request_options)
        return _response.data


class AsyncPostCallAnalysisClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPostCallAnalysisClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPostCallAnalysisClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPostCallAnalysisClient
        """
        return self._raw_client

    async def analyze(
        self,
        *,
        transcript: str,
        disposition_metrics: typing.Optional[typing.Sequence[DispositionMetric]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PcaResponse:
        """
        Analyze a call transcript and extract structured disposition metrics. Provide the
        transcript and the metrics you want extracted; the response returns a short summary
        plus one result per requested metric, each with a value, a confidence score, and the
        transcript evidence for it. Values are grounded strictly in the transcript (no
        inference); when evidence is absent, the type's fallback is used.

        Parameters
        ----------
        transcript : str
            The full call transcript to analyze.

        disposition_metrics : typing.Optional[typing.Sequence[DispositionMetric]]
            The metrics to extract. Omit or pass an empty array to get just a summary.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PcaResponse
            Analysis complete.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI
        from smallestai.waves import DispositionMetric

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.waves.post_call_analysis.analyze(
                transcript="User: I want a refund for my torn jacket. Agent: I have logged your complaint and issued a full refund of $80.",
                disposition_metrics=[
                    DispositionMetric(
                        identifier="refund_issued",
                        disposition_metric_prompt="Was a refund issued to the customer?",
                        disposition_metric_type="BOOLEAN",
                    )
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.analyze(
            transcript=transcript, disposition_metrics=disposition_metrics, request_options=request_options
        )
        return _response.data

    async def generate(
        self, *, prompt: str, request_options: typing.Optional[RequestOptions] = None
    ) -> GeneratePostCallAnalysisResponse:
        """
        Run a single-prompt generation and get the model's text output. A lightweight
        text-in / text-out endpoint (no chat history); for multi-turn chat use the
        Electron chat completions API instead.

        Parameters
        ----------
        prompt : str
            The prompt to generate from.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeneratePostCallAnalysisResponse
            Generation complete.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.waves.post_call_analysis.generate(
                prompt="Say hello in exactly three words.",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.generate(prompt=prompt, request_options=request_options)
        return _response.data
