# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ....core.pydantic_utilities import IS_PYDANTIC_V2
from ....core.serialization import FieldMetadata
from ....core.unchecked_base_model import UncheckedBaseModel
from .lightning_v31tts_request_message_language import LightningV31TtsRequestMessageLanguage
from .lightning_v31tts_request_message_model import LightningV31TtsRequestMessageModel


class LightningV31TtsRequestMessage(UncheckedBaseModel):
    voice_id: str = pydantic.Field()
    """
    The ID of the voice to use
    """

    text: str = pydantic.Field()
    """
    The text to convert to speech
    """

    model: typing.Optional[LightningV31TtsRequestMessageModel] = pydantic.Field(default=None)
    """
    TTS model to route the request to.
    
    - `lightning_v3.1` (default) — standard Lightning v3.1 pool.
    - `lightning_v3.1_pro` — Lightning v3.1 Pro pool with a curated
      voice catalog. See the
      [Pro model card](/waves/model-cards/text-to-speech/lightning-v-3-1-pro).
    
    New integrations should connect to the unified
    `/waves/v1/tts/live` WebSocket instead of this endpoint, but the
    `model` field is supported here for backwards-compatible Pro
    opt-in.
    """

    max_buffer_flush_ms: typing.Optional[int] = pydantic.Field(default=None)
    """
    The maximum time (in ms) to wait for more input before generating output. It flushes when either this time is reached or enough input is received for optimal output—whichever comes first. This is useful for input streams. Defaults to 0
    """

    continue_: typing_extensions.Annotated[
        typing.Optional[bool],
        FieldMetadata(alias="continue"),
        pydantic.Field(
            alias="continue",
            description="This setting controls whether the system should buffer and wait for more input after receiving the current one. If not set, it assumes no more input is coming.",
        ),
    ] = None
    flush: typing.Optional[bool] = pydantic.Field(default=None)
    """
    This setting controls whether the system should flush the current buffer.
    """

    complete_backoff_ms: typing.Optional[float] = pydantic.Field(default=None)
    """
    The time in ms to wait after the last chunk is sent before sending the complete response. Default is 4000ms. Maximum is 10000ms.
    """

    language: typing.Optional[LightningV31TtsRequestMessageLanguage] = pydantic.Field(default=None)
    """
    Language code for synthesis. Influences pronunciation, number/date
    normalization, and phoneme selection.
    
    - Indian: `en`, `hi`, `mr`, `kn`, `ta`, `bn`, `gu`, `te`, `ml`, `pa`, `or`
    - European: `es` (Spanish)
    """

    sample_rate: typing.Optional[int] = pydantic.Field(default=None)
    """
    Audio sample rate in Hz. Supported values: 8000, 16000, 24000, 44100
    """

    speed: typing.Optional[float] = pydantic.Field(default=None)
    """
    Speaking speed multiplier
    """

    session_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Optional client-provided session identifier for correlation. Only alphanumeric characters, hyphens, underscores, and dots allowed. Max 128 characters. Echoed back in responses as `external_session_id`.
    """

    request_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Optional client-provided request identifier for correlation. Only alphanumeric characters, hyphens, underscores, and dots allowed. Max 128 characters. Echoed back in responses as `external_request_id`.
    """

    word_timestamps: typing.Optional[bool] = pydantic.Field(default=None)
    """
    Opt in to per-word timing events for the synthesized audio. When `true`, the server interleaves `status: "word_timestamp"` frames with the audio `chunk` frames; each carries `data: { id, word, start, end }` where `start`/`end` are floats in seconds relative to the start of the audio stream, and `word` is verbatim from the input text (un-normalized — `"$100"` stays `"$100"`, not `"one hundred dollars"`). Supported on base-queue English + Hindi voices (`meher`, `devansh`, `kartik`, `maithili`, `liam`, `avery`); other voice families silently emit no word events (audio still works). Defaults to `false` so existing integrations see no change.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
