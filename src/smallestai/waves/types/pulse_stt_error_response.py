# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ...core.pydantic_utilities import IS_PYDANTIC_V2
from ...core.unchecked_base_model import UncheckedBaseModel
from .pulse_stt_error_response_status import PulseSttErrorResponseStatus


class PulseSttErrorResponse(UncheckedBaseModel):
    """
    Canonical error shape for 400-class responses from the Pulse STT REST
    endpoint. Live-probed against prod 2026-06-16.

    Two variants share the same envelope:
    - Generic validation: `{errors, status, message}` (e.g. invalid params,
      malformed body, oversized upload).
    - Discriminated: `{status, error_code, message, language?, region?}`
      for known classes such as `LANGUAGE_NOT_ENABLED_IN_REGION`.

    Clients should treat `errors` and `error_code` as mutually exclusive —
    exactly one will be present. `status` and `message` are always set.

    Note: the platform's field naming is inconsistent — `errors` (plural)
    on 400-class responses vs `error` (singular) on some 401 responses.
    See `PulseSttErrorResponseLegacy` for the singular-`error` shape.
    """

    status: PulseSttErrorResponseStatus = pydantic.Field()
    """
    Always `"error"`.
    """

    message: str = pydantic.Field()
    """
    Generic error category (e.g. "Error handling audio data").
    """

    errors: typing.Optional[str] = pydantic.Field(default=None)
    """
    Human-readable error message (validation variant).
    """

    error_code: typing.Optional[str] = pydantic.Field(default=None)
    """
    Machine-readable error code (discriminated variant, e.g. `LANGUAGE_NOT_ENABLED_IN_REGION`).
    """

    language: typing.Optional[str] = pydantic.Field(default=None)
    """
    Echo of the requested language (present on `LANGUAGE_NOT_ENABLED_IN_REGION`).
    """

    region: typing.Optional[str] = pydantic.Field(default=None)
    """
    Region the request hit (present on `LANGUAGE_NOT_ENABLED_IN_REGION`).
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
