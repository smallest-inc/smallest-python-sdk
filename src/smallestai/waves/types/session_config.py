# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ...core.pydantic_utilities import IS_PYDANTIC_V2
from ...core.unchecked_base_model import UncheckedBaseModel
from .session_config_voice import SessionConfigVoice
from .tool import Tool


class SessionConfig(UncheckedBaseModel):
    """
    Sent inside the client's `session.configure` frame and echoed inside the server's `session.configured` frame. All fields optional — omitted fields fall back to the server default.
    """

    instructions: typing.Optional[str] = pydantic.Field(default=None)
    """
    Persona / system prompt that defines the assistant's behaviour.
    """

    voice: typing.Optional[SessionConfigVoice] = pydantic.Field(default=None)
    """
    Voice identifier.
    """

    tools: typing.Optional[typing.List[Tool]] = pydantic.Field(default=None)
    """
    Function-calling tool schemas the model may invoke.
    """

    generate_initial_response: typing.Optional[bool] = pydantic.Field(default=None)
    """
    When `true`, the model speaks first instead of waiting for user input. Honoured only at handshake.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
