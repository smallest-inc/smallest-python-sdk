# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ....core.pydantic_utilities import IS_PYDANTIC_V2
from ....core.unchecked_base_model import UncheckedBaseModel


class TtsResponseMessageData(UncheckedBaseModel):
    """
    Frame-specific payload. Shape depends on `status` — see the per-frame examples below.
    """

    audio: typing.Optional[str] = pydantic.Field(default=None)
    """
    Base64-encoded audio chunk (present on `status: "chunk"` frames).
    """

    id: typing.Optional[int] = pydantic.Field(default=None)
    """
    0-indexed position of the word within the input text (present on `status: "word_timestamp"` frames).
    """

    word: typing.Optional[str] = pydantic.Field(default=None)
    """
    Exact substring from the input text, un-normalized — `"$100"` stays `"$100"`, `"25th"` stays `"25th"` (present on `status: "word_timestamp"` frames).
    """

    start: typing.Optional[float] = pydantic.Field(default=None)
    """
    Start of the word in seconds, relative to the start of the audio stream (present on `status: "word_timestamp"` frames).
    """

    end: typing.Optional[float] = pydantic.Field(default=None)
    """
    End of the word in seconds, relative to the start of the audio stream (present on `status: "word_timestamp"` frames).
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
