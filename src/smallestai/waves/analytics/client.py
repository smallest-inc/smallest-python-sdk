# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from ..types.count_timeseries_response import CountTimeseriesResponse
from ..types.credits_timeseries_response import CreditsTimeseriesResponse
from ..types.streaming_speech_to_text_logs_response import StreamingSpeechToTextLogsResponse
from ..types.text_to_speech_logs_response import TextToSpeechLogsResponse
from ..types.values_timeseries_response import ValuesTimeseriesResponse
from ..types.webhook_logs_response import WebhookLogsResponse
from .raw_client import AsyncRawAnalyticsClient, RawAnalyticsClient
from .types.delete_streaming_speech_to_text_history_response import DeleteStreamingSpeechToTextHistoryResponse
from .types.list_webhook_logs_request_status import ListWebhookLogsRequestStatus


class AnalyticsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAnalyticsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAnalyticsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAnalyticsClient
        """
        return self._raw_client

    def list_streaming_speech_to_text_logs(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        text: typing.Optional[str] = None,
        language: typing.Optional[str] = None,
        model: typing.Optional[str] = None,
        duration_min: typing.Optional[float] = None,
        duration_max: typing.Optional[float] = None,
        timestamp_from: typing.Optional[int] = None,
        timestamp_to: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamingSpeechToTextLogsResponse:
        """
        Paginated log of Speech to Text requests for the calling organization. Each entry
        records the model used, the transcript, the request parameters that were on the
        wire (word timestamps, diarization, PII / PCI redaction, keyword boosting), and
        any downstream signals the request opted into (emotion, gender, keyword hits).
        Use this to audit request-level activity or to reconcile with your own logs.

        Parameters
        ----------
        page : typing.Optional[int]
            1-indexed page number.

        page_size : typing.Optional[int]
            Number of records per page. Max 50.

        text : typing.Optional[str]
            Case-insensitive substring match against the transcription.

        language : typing.Optional[str]
            Exact-match filter on language code (e.g. `en`, `hi`).

        model : typing.Optional[str]
            Exact-match filter on STT model (e.g. `pulse`).

        duration_min : typing.Optional[float]
            Minimum audio duration in seconds.

        duration_max : typing.Optional[float]
            Maximum audio duration in seconds.

        timestamp_from : typing.Optional[int]
            Lower bound on request timestamp, Unix seconds.

        timestamp_to : typing.Optional[int]
            Upper bound on request timestamp, Unix seconds.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamingSpeechToTextLogsResponse
            STT logs retrieved successfully.

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.waves.analytics.list_streaming_speech_to_text_logs()
        """
        _response = self._raw_client.list_streaming_speech_to_text_logs(
            page=page,
            page_size=page_size,
            text=text,
            language=language,
            model=model,
            duration_min=duration_min,
            duration_max=duration_max,
            timestamp_from=timestamp_from,
            timestamp_to=timestamp_to,
            request_options=request_options,
        )
        return _response.data

    def delete_streaming_speech_to_text_history(
        self, request_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeleteStreamingSpeechToTextHistoryResponse:
        """
        Soft-deletes a single Speech to Text request from the org's analytics history.
        Idempotent: a `request_id` that does not exist still returns `success`. Use this
        to redact a specific transcript from the dashboard log view.

        Parameters
        ----------
        request_id : str
            The `request_id` (UUID) of the STT request to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteStreamingSpeechToTextHistoryResponse
            History entry deleted (or was already absent).

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.waves.analytics.delete_streaming_speech_to_text_history(
            request_id="3eea9859-609b-45c5-8a25-0337c9763c96",
        )
        """
        _response = self._raw_client.delete_streaming_speech_to_text_history(
            request_id, request_options=request_options
        )
        return _response.data

    def get_streaming_speech_to_text_usage_timeseries(
        self,
        *,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CountTimeseriesResponse:
        """
        Bucketed request count for Speech to Text over a datetime range. Use for a
        traffic chart on the STT dashboard, or to reconcile daily usage.

        Parameters
        ----------
        from_ : typing.Optional[dt.datetime]
            Start of the range, ISO 8601 datetime (e.g. `2026-08-01T00:00:00Z`).

        to : typing.Optional[dt.datetime]
            End of the range, ISO 8601 datetime (e.g. `2026-08-07T00:00:00Z`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CountTimeseriesResponse
            Timeseries retrieved successfully.

        Examples
        --------
        import datetime

        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.waves.analytics.get_streaming_speech_to_text_usage_timeseries(
            from_=datetime.datetime.fromisoformat(
                "2026-08-01 00:00:00+00:00",
            ),
            to=datetime.datetime.fromisoformat(
                "2026-08-07 00:00:00+00:00",
            ),
        )
        """
        _response = self._raw_client.get_streaming_speech_to_text_usage_timeseries(
            from_=from_, to=to, request_options=request_options
        )
        return _response.data

    def list_text_to_speech_logs(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        text: typing.Optional[str] = None,
        credits_min: typing.Optional[float] = None,
        credits_max: typing.Optional[float] = None,
        timestamp_from: typing.Optional[int] = None,
        timestamp_to: typing.Optional[int] = None,
        model: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> TextToSpeechLogsResponse:
        """
        Paginated log of Text to Speech requests. Each entry records the model, voice,
        input text length, sample rate, output format, and credits consumed.

        Parameters
        ----------
        page : typing.Optional[int]
            1-indexed page number.

        page_size : typing.Optional[int]
            Number of records per page. Max 50.

        text : typing.Optional[str]
            Case-insensitive substring match against the input text.

        credits_min : typing.Optional[float]
            Minimum credits consumed by the request.

        credits_max : typing.Optional[float]
            Maximum credits consumed by the request.

        timestamp_from : typing.Optional[int]
            Lower bound on request timestamp, Unix seconds.

        timestamp_to : typing.Optional[int]
            Upper bound on request timestamp, Unix seconds.

        model : typing.Optional[str]
            Exact-match filter on TTS model (e.g. `lightning-v3.1-pro`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TextToSpeechLogsResponse
            TTS logs retrieved successfully.

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.waves.analytics.list_text_to_speech_logs()
        """
        _response = self._raw_client.list_text_to_speech_logs(
            page=page,
            page_size=page_size,
            text=text,
            credits_min=credits_min,
            credits_max=credits_max,
            timestamp_from=timestamp_from,
            timestamp_to=timestamp_to,
            model=model,
            request_options=request_options,
        )
        return _response.data

    def get_text_to_speech_usage_timeseries(
        self,
        *,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CountTimeseriesResponse:
        """
        Bucketed request count for Text to Speech over a datetime range.

        Parameters
        ----------
        from_ : typing.Optional[dt.datetime]
            Start of the range, ISO 8601 datetime (e.g. `2026-08-01T00:00:00Z`).

        to : typing.Optional[dt.datetime]
            End of the range, ISO 8601 datetime (e.g. `2026-08-07T00:00:00Z`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CountTimeseriesResponse
            Timeseries retrieved successfully.

        Examples
        --------
        import datetime

        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.waves.analytics.get_text_to_speech_usage_timeseries(
            from_=datetime.datetime.fromisoformat(
                "2026-08-01 00:00:00+00:00",
            ),
            to=datetime.datetime.fromisoformat(
                "2026-08-07 00:00:00+00:00",
            ),
        )
        """
        _response = self._raw_client.get_text_to_speech_usage_timeseries(
            from_=from_, to=to, request_options=request_options
        )
        return _response.data

    def get_text_to_speech_credits_timeseries(
        self,
        *,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreditsTimeseriesResponse:
        """
        Bucketed credit spend for Text to Speech over a datetime range. Use this to
        forecast spend or reconcile bills.

        Parameters
        ----------
        from_ : typing.Optional[dt.datetime]
            Start of the range, ISO 8601 datetime (e.g. `2026-08-01T00:00:00Z`).

        to : typing.Optional[dt.datetime]
            End of the range, ISO 8601 datetime (e.g. `2026-08-07T00:00:00Z`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreditsTimeseriesResponse
            Credit timeseries retrieved successfully.

        Examples
        --------
        import datetime

        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.waves.analytics.get_text_to_speech_credits_timeseries(
            from_=datetime.datetime.fromisoformat(
                "2026-08-01 00:00:00+00:00",
            ),
            to=datetime.datetime.fromisoformat(
                "2026-08-07 00:00:00+00:00",
            ),
        )
        """
        _response = self._raw_client.get_text_to_speech_credits_timeseries(
            from_=from_, to=to, request_options=request_options
        )
        return _response.data

    def get_text_to_speech_concurrency_timeseries(
        self,
        *,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ValuesTimeseriesResponse:
        """
        Bucketed peak concurrency (simultaneous active TTS requests) over a datetime
        range. Use to size against your account concurrency cap.

        Parameters
        ----------
        from_ : typing.Optional[dt.datetime]
            Start of the range, ISO 8601 datetime (e.g. `2026-08-01T00:00:00Z`).

        to : typing.Optional[dt.datetime]
            End of the range, ISO 8601 datetime (e.g. `2026-08-07T00:00:00Z`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ValuesTimeseriesResponse
            Concurrency timeseries retrieved successfully.

        Examples
        --------
        import datetime

        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.waves.analytics.get_text_to_speech_concurrency_timeseries(
            from_=datetime.datetime.fromisoformat(
                "2026-08-01 00:00:00+00:00",
            ),
            to=datetime.datetime.fromisoformat(
                "2026-08-07 00:00:00+00:00",
            ),
        )
        """
        _response = self._raw_client.get_text_to_speech_concurrency_timeseries(
            from_=from_, to=to, request_options=request_options
        )
        return _response.data

    def get_text_to_speech_websocket_connections_timeseries(
        self,
        *,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ValuesTimeseriesResponse:
        """
        Bucketed count of open TTS WebSocket connections over a datetime range.

        Parameters
        ----------
        from_ : typing.Optional[dt.datetime]
            Start of the range, ISO 8601 datetime (e.g. `2026-08-01T00:00:00Z`).

        to : typing.Optional[dt.datetime]
            End of the range, ISO 8601 datetime (e.g. `2026-08-07T00:00:00Z`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ValuesTimeseriesResponse
            WebSocket-connection timeseries retrieved successfully.

        Examples
        --------
        import datetime

        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.waves.analytics.get_text_to_speech_websocket_connections_timeseries(
            from_=datetime.datetime.fromisoformat(
                "2026-08-01 00:00:00+00:00",
            ),
            to=datetime.datetime.fromisoformat(
                "2026-08-07 00:00:00+00:00",
            ),
        )
        """
        _response = self._raw_client.get_text_to_speech_websocket_connections_timeseries(
            from_=from_, to=to, request_options=request_options
        )
        return _response.data

    def list_webhook_logs(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        timestamp_from: typing.Optional[int] = None,
        timestamp_to: typing.Optional[int] = None,
        status: typing.Optional[ListWebhookLogsRequestStatus] = None,
        webhook_url: typing.Optional[str] = None,
        event_type: typing.Optional[str] = None,
        request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> WebhookLogsResponse:
        """
        Paginated log of webhook delivery attempts (e.g. `asr.completed` callbacks).
        Each entry records the destination URL, HTTP method, event type, delivery
        status, attempt number, response status code, and any error.

        Parameters
        ----------
        page : typing.Optional[int]
            1-indexed page number.

        page_size : typing.Optional[int]
            Number of records per page. Max 50.

        timestamp_from : typing.Optional[int]
            Lower bound on delivery timestamp, Unix seconds.

        timestamp_to : typing.Optional[int]
            Upper bound on delivery timestamp, Unix seconds.

        status : typing.Optional[ListWebhookLogsRequestStatus]
            Delivery-outcome filter.

        webhook_url : typing.Optional[str]
            Case-insensitive substring match against the destination URL.

        event_type : typing.Optional[str]
            Exact-match filter on event type (e.g. `asr.completed`).

        request_id : typing.Optional[str]
            Case-insensitive substring match against the source request id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WebhookLogsResponse
            Webhook logs retrieved successfully.

        Examples
        --------
        from smallestai import SmallestAI

        client = SmallestAI(
            api_key="YOUR_API_KEY",
        )
        client.waves.analytics.list_webhook_logs()
        """
        _response = self._raw_client.list_webhook_logs(
            page=page,
            page_size=page_size,
            timestamp_from=timestamp_from,
            timestamp_to=timestamp_to,
            status=status,
            webhook_url=webhook_url,
            event_type=event_type,
            request_id=request_id,
            request_options=request_options,
        )
        return _response.data


class AsyncAnalyticsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAnalyticsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAnalyticsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAnalyticsClient
        """
        return self._raw_client

    async def list_streaming_speech_to_text_logs(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        text: typing.Optional[str] = None,
        language: typing.Optional[str] = None,
        model: typing.Optional[str] = None,
        duration_min: typing.Optional[float] = None,
        duration_max: typing.Optional[float] = None,
        timestamp_from: typing.Optional[int] = None,
        timestamp_to: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> StreamingSpeechToTextLogsResponse:
        """
        Paginated log of Speech to Text requests for the calling organization. Each entry
        records the model used, the transcript, the request parameters that were on the
        wire (word timestamps, diarization, PII / PCI redaction, keyword boosting), and
        any downstream signals the request opted into (emotion, gender, keyword hits).
        Use this to audit request-level activity or to reconcile with your own logs.

        Parameters
        ----------
        page : typing.Optional[int]
            1-indexed page number.

        page_size : typing.Optional[int]
            Number of records per page. Max 50.

        text : typing.Optional[str]
            Case-insensitive substring match against the transcription.

        language : typing.Optional[str]
            Exact-match filter on language code (e.g. `en`, `hi`).

        model : typing.Optional[str]
            Exact-match filter on STT model (e.g. `pulse`).

        duration_min : typing.Optional[float]
            Minimum audio duration in seconds.

        duration_max : typing.Optional[float]
            Maximum audio duration in seconds.

        timestamp_from : typing.Optional[int]
            Lower bound on request timestamp, Unix seconds.

        timestamp_to : typing.Optional[int]
            Upper bound on request timestamp, Unix seconds.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        StreamingSpeechToTextLogsResponse
            STT logs retrieved successfully.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.waves.analytics.list_streaming_speech_to_text_logs()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_streaming_speech_to_text_logs(
            page=page,
            page_size=page_size,
            text=text,
            language=language,
            model=model,
            duration_min=duration_min,
            duration_max=duration_max,
            timestamp_from=timestamp_from,
            timestamp_to=timestamp_to,
            request_options=request_options,
        )
        return _response.data

    async def delete_streaming_speech_to_text_history(
        self, request_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeleteStreamingSpeechToTextHistoryResponse:
        """
        Soft-deletes a single Speech to Text request from the org's analytics history.
        Idempotent: a `request_id` that does not exist still returns `success`. Use this
        to redact a specific transcript from the dashboard log view.

        Parameters
        ----------
        request_id : str
            The `request_id` (UUID) of the STT request to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteStreamingSpeechToTextHistoryResponse
            History entry deleted (or was already absent).

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.waves.analytics.delete_streaming_speech_to_text_history(
                request_id="3eea9859-609b-45c5-8a25-0337c9763c96",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_streaming_speech_to_text_history(
            request_id, request_options=request_options
        )
        return _response.data

    async def get_streaming_speech_to_text_usage_timeseries(
        self,
        *,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CountTimeseriesResponse:
        """
        Bucketed request count for Speech to Text over a datetime range. Use for a
        traffic chart on the STT dashboard, or to reconcile daily usage.

        Parameters
        ----------
        from_ : typing.Optional[dt.datetime]
            Start of the range, ISO 8601 datetime (e.g. `2026-08-01T00:00:00Z`).

        to : typing.Optional[dt.datetime]
            End of the range, ISO 8601 datetime (e.g. `2026-08-07T00:00:00Z`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CountTimeseriesResponse
            Timeseries retrieved successfully.

        Examples
        --------
        import asyncio
        import datetime

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.waves.analytics.get_streaming_speech_to_text_usage_timeseries(
                from_=datetime.datetime.fromisoformat(
                    "2026-08-01 00:00:00+00:00",
                ),
                to=datetime.datetime.fromisoformat(
                    "2026-08-07 00:00:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_streaming_speech_to_text_usage_timeseries(
            from_=from_, to=to, request_options=request_options
        )
        return _response.data

    async def list_text_to_speech_logs(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        text: typing.Optional[str] = None,
        credits_min: typing.Optional[float] = None,
        credits_max: typing.Optional[float] = None,
        timestamp_from: typing.Optional[int] = None,
        timestamp_to: typing.Optional[int] = None,
        model: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> TextToSpeechLogsResponse:
        """
        Paginated log of Text to Speech requests. Each entry records the model, voice,
        input text length, sample rate, output format, and credits consumed.

        Parameters
        ----------
        page : typing.Optional[int]
            1-indexed page number.

        page_size : typing.Optional[int]
            Number of records per page. Max 50.

        text : typing.Optional[str]
            Case-insensitive substring match against the input text.

        credits_min : typing.Optional[float]
            Minimum credits consumed by the request.

        credits_max : typing.Optional[float]
            Maximum credits consumed by the request.

        timestamp_from : typing.Optional[int]
            Lower bound on request timestamp, Unix seconds.

        timestamp_to : typing.Optional[int]
            Upper bound on request timestamp, Unix seconds.

        model : typing.Optional[str]
            Exact-match filter on TTS model (e.g. `lightning-v3.1-pro`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TextToSpeechLogsResponse
            TTS logs retrieved successfully.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.waves.analytics.list_text_to_speech_logs()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_text_to_speech_logs(
            page=page,
            page_size=page_size,
            text=text,
            credits_min=credits_min,
            credits_max=credits_max,
            timestamp_from=timestamp_from,
            timestamp_to=timestamp_to,
            model=model,
            request_options=request_options,
        )
        return _response.data

    async def get_text_to_speech_usage_timeseries(
        self,
        *,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CountTimeseriesResponse:
        """
        Bucketed request count for Text to Speech over a datetime range.

        Parameters
        ----------
        from_ : typing.Optional[dt.datetime]
            Start of the range, ISO 8601 datetime (e.g. `2026-08-01T00:00:00Z`).

        to : typing.Optional[dt.datetime]
            End of the range, ISO 8601 datetime (e.g. `2026-08-07T00:00:00Z`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CountTimeseriesResponse
            Timeseries retrieved successfully.

        Examples
        --------
        import asyncio
        import datetime

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.waves.analytics.get_text_to_speech_usage_timeseries(
                from_=datetime.datetime.fromisoformat(
                    "2026-08-01 00:00:00+00:00",
                ),
                to=datetime.datetime.fromisoformat(
                    "2026-08-07 00:00:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_text_to_speech_usage_timeseries(
            from_=from_, to=to, request_options=request_options
        )
        return _response.data

    async def get_text_to_speech_credits_timeseries(
        self,
        *,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreditsTimeseriesResponse:
        """
        Bucketed credit spend for Text to Speech over a datetime range. Use this to
        forecast spend or reconcile bills.

        Parameters
        ----------
        from_ : typing.Optional[dt.datetime]
            Start of the range, ISO 8601 datetime (e.g. `2026-08-01T00:00:00Z`).

        to : typing.Optional[dt.datetime]
            End of the range, ISO 8601 datetime (e.g. `2026-08-07T00:00:00Z`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreditsTimeseriesResponse
            Credit timeseries retrieved successfully.

        Examples
        --------
        import asyncio
        import datetime

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.waves.analytics.get_text_to_speech_credits_timeseries(
                from_=datetime.datetime.fromisoformat(
                    "2026-08-01 00:00:00+00:00",
                ),
                to=datetime.datetime.fromisoformat(
                    "2026-08-07 00:00:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_text_to_speech_credits_timeseries(
            from_=from_, to=to, request_options=request_options
        )
        return _response.data

    async def get_text_to_speech_concurrency_timeseries(
        self,
        *,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ValuesTimeseriesResponse:
        """
        Bucketed peak concurrency (simultaneous active TTS requests) over a datetime
        range. Use to size against your account concurrency cap.

        Parameters
        ----------
        from_ : typing.Optional[dt.datetime]
            Start of the range, ISO 8601 datetime (e.g. `2026-08-01T00:00:00Z`).

        to : typing.Optional[dt.datetime]
            End of the range, ISO 8601 datetime (e.g. `2026-08-07T00:00:00Z`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ValuesTimeseriesResponse
            Concurrency timeseries retrieved successfully.

        Examples
        --------
        import asyncio
        import datetime

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.waves.analytics.get_text_to_speech_concurrency_timeseries(
                from_=datetime.datetime.fromisoformat(
                    "2026-08-01 00:00:00+00:00",
                ),
                to=datetime.datetime.fromisoformat(
                    "2026-08-07 00:00:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_text_to_speech_concurrency_timeseries(
            from_=from_, to=to, request_options=request_options
        )
        return _response.data

    async def get_text_to_speech_websocket_connections_timeseries(
        self,
        *,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ValuesTimeseriesResponse:
        """
        Bucketed count of open TTS WebSocket connections over a datetime range.

        Parameters
        ----------
        from_ : typing.Optional[dt.datetime]
            Start of the range, ISO 8601 datetime (e.g. `2026-08-01T00:00:00Z`).

        to : typing.Optional[dt.datetime]
            End of the range, ISO 8601 datetime (e.g. `2026-08-07T00:00:00Z`).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ValuesTimeseriesResponse
            WebSocket-connection timeseries retrieved successfully.

        Examples
        --------
        import asyncio
        import datetime

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.waves.analytics.get_text_to_speech_websocket_connections_timeseries(
                from_=datetime.datetime.fromisoformat(
                    "2026-08-01 00:00:00+00:00",
                ),
                to=datetime.datetime.fromisoformat(
                    "2026-08-07 00:00:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_text_to_speech_websocket_connections_timeseries(
            from_=from_, to=to, request_options=request_options
        )
        return _response.data

    async def list_webhook_logs(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        timestamp_from: typing.Optional[int] = None,
        timestamp_to: typing.Optional[int] = None,
        status: typing.Optional[ListWebhookLogsRequestStatus] = None,
        webhook_url: typing.Optional[str] = None,
        event_type: typing.Optional[str] = None,
        request_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> WebhookLogsResponse:
        """
        Paginated log of webhook delivery attempts (e.g. `asr.completed` callbacks).
        Each entry records the destination URL, HTTP method, event type, delivery
        status, attempt number, response status code, and any error.

        Parameters
        ----------
        page : typing.Optional[int]
            1-indexed page number.

        page_size : typing.Optional[int]
            Number of records per page. Max 50.

        timestamp_from : typing.Optional[int]
            Lower bound on delivery timestamp, Unix seconds.

        timestamp_to : typing.Optional[int]
            Upper bound on delivery timestamp, Unix seconds.

        status : typing.Optional[ListWebhookLogsRequestStatus]
            Delivery-outcome filter.

        webhook_url : typing.Optional[str]
            Case-insensitive substring match against the destination URL.

        event_type : typing.Optional[str]
            Exact-match filter on event type (e.g. `asr.completed`).

        request_id : typing.Optional[str]
            Case-insensitive substring match against the source request id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WebhookLogsResponse
            Webhook logs retrieved successfully.

        Examples
        --------
        import asyncio

        from smallestai import AsyncSmallestAI

        client = AsyncSmallestAI(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.waves.analytics.list_webhook_logs()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_webhook_logs(
            page=page,
            page_size=page_size,
            timestamp_from=timestamp_from,
            timestamp_to=timestamp_to,
            status=status,
            webhook_url=webhook_url,
            event_type=event_type,
            request_id=request_id,
            request_options=request_options,
        )
        return _response.data
